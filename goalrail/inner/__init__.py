"""Pre-integration goalrail code. Moved here during unification.

Internal imports between files here use relative syntax (from .X import
Y). Cross-boundary imports from goalrail-native code inside
goalrail/ use the explicit path (goalrail.inner.X) so the
dependency is grep-findable.
"""
