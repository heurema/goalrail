apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: goalrail-postgres
    component: postgres
spec:
  type: ClusterIP
  selector:
    app: goalrail-postgres
    component: postgres
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
