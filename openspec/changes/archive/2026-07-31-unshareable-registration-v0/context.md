# Context Pack

- **Context Pack ID:** context-unshareable-registration-v0
- **Version:** 1
- **Previous version:** none
- **Started at:** 2026-07-31T11:55:00Z
- **Completed at:** 2026-07-31T13:05:00Z
- **Outcome:** sufficient

## Context Items

| ID | Kind | Claim | Source | Observed at | Relevance |
|---|---|---|---|---|---|
| CTX-1 | repository | On a machine carrying no personal global ignore rule, `gr init` refuses the registration: `registration.applied` is `false` and the refusal names the missing `.gitignore` entry and `--fix-gitignore`. The rest of the harness installs, so the tool is present and the attachment never fires. | `env HOME=<fresh> XDG_CONFIG_HOME=<fresh> gr init` in a fresh clone of a kata repository | 2026-07-31T12:07:00Z | This is the defect. It is the first thing a new user meets, and it leaves them with a tool that reports success and does nothing. |
| CTX-2 | repository | The owner's own machine cannot reproduce it. `~/.config/git/ignore` carries `**/.claude/settings.local.json`, so their registration applies. Reproducing a new user's state needs a fabricated `HOME` and `XDG_CONFIG_HOME`. | `git check-ignore -v .claude/settings.local.json` → `/Users/vi/.config/git/ignore:1` | 2026-07-31T12:06:00Z | Explains why the defect survived every local run. Any verification of this change must fabricate the environment or it proves nothing. |
| CTX-3 | repository | Goalrail's own repository reproduces the defect: its tracked `.gitignore` lists `.goalrail/` but not `.claude/settings.local.json`. | `openspec/../.gitignore` read directly | 2026-07-31T12:40:00Z | The project cannot dogfood its own initialization on a clean machine. |
| CTX-4 | repository | The read path needs no change at all. `IgnoreState` asks git rather than parsing a file — `git -C <root> check-ignore -q -- <path>` at `internal/ambient/scope.go:346` — and git honours the per-clone exclude file. Seeding that file by hand flips `applied` from `false` to `true` with no code changed. | `internal/ambient/scope.go:338-356`; hand-seeded exclude then `gr init` | 2026-07-31T12:35:00Z | Bounds the change: this is a writer change, not a redesign. The half that decides whether a path is unshareable already accepts the answer. |
| CTX-5 | repository | No promoted requirement names `.gitignore`, `git check-ignore`, or the exclude file. The promoted vocabulary is provider-neutral: "ignored by the version control system", "the repository's ignore rules", "the exact ignore entry". | `grep -rn "gitignore\|check-ignore\|info/exclude" openspec/specs/` → no output | 2026-07-31T12:20:00Z | Which file carries the rule is an implementation detail no promise fixes. The delta is about consent, not about a filename. |
| CTX-6 | repository | The behaviour is nonetheless pinned, by three sentences in one requirement — `openspec/specs/harness-init/spec.md:107-114`, "Initialization MUST NOT edit the repository's ignore rules unasked" and "An explicit flag SHALL add the missing entries" — and by two of its scenarios at L153-159. | `openspec/specs/harness-init/spec.md:85-163` | 2026-07-31T12:20:00Z | The change needs a MODIFIED delta on exactly one requirement in one file. It is not a free implementation swap. |
| CTX-7 | repository | The neighbouring promoted capability already permits the new behaviour and stays true under it. `openspec/specs/ambient-connect/spec.md:79-87` requires registration "only where that path is unshareable — ignored by the version control system, made so by the same act where that is possible", and conditions its refusal on the path not being makeable unshareable. | `openspec/specs/ambient-connect/spec.md:79-87` | 2026-07-31T12:45:00Z | No delta is owed there. The clause was written permissively and the new act is the case it anticipated. |
| CTX-8 | external | `.git` is a regular file, not a directory, in linked worktrees and in submodules. A writer that joins `<root>/.git/info/exclude` is broken in both. The location has to be asked of git — `git rev-parse --git-common-dir`. | `git worktree add`; `git submodule add`; `rev-parse` in each | 2026-07-31T12:50:00Z | The most likely way to implement this wrong, and it fails silently in exactly the setups a working developer has. |
| CTX-9 | external | `.git/info/` is not guaranteed to exist. `git init` does not create it under the owner's own `init.templateDir`, so the writer must create the parent directory rather than assume it. | `git init` in a scratch directory, then `ls .git/info` → absent | 2026-07-31T12:50:00Z | Today's writer targets `<root>/.gitignore`, whose parent always exists. The new one inherits a precondition the old one never had. |
| CTX-10 | external | A tracked `.gitignore` outranks the per-clone exclude. A negation there — `!.claude/**`, `!*.json`, or a nested `.claude/.gitignore` — makes the exclude a silent no-op, and only an entry in the tracked file restores the ignore. | `git check-ignore -v` with each negation in turn; `-q` exit 1 under the negation, 0 after appending the entry to the tracked file | 2026-07-31T12:52:00Z | Settles the flag question: `--fix-gitignore` is the only remedy in these repositories. Retiring it would be a capability regression, not a simplification. |
| CTX-11 | external | The user-global ignore file ranks below the per-clone exclude and covers every repository the user will ever clone. It is what hides the defect on the owner's machine, and it mutates no repository at all. | `GIT_CONFIG_GLOBAL=/dev/null` with the real `XDG_CONFIG_HOME` still ignores; with an empty one the file stages | 2026-07-31T12:52:00Z | The most durable of the three locations, and the one this change must decline: it grants a repository-scoped act permission over every other repository. |
| CTX-12 | repository | A prototype that writes the exclude unconditionally breaks two existing behaviours, not two stale pins: `gr init` in a directory that is not a repository fails with `resolve git dir: exit status 128`, and `gr init` with an empty lookup path fails because `git` is not found. Three other test failures are genuine pins to rewrite. | Prototype in a copy of the repository; `go test ./...` → `TestInitCommandIsExplicitAndReportsItself`, `TestTheHarnessCommandsNeedNoNodeRuntime` | 2026-07-31T12:55:00Z | The write must fail open. Initialization does not require git today and must not start requiring it. |
| CTX-13 | external | In a bare repository `git check-ignore` exits 128; goalrail reads that as a plain "not ignored". `gr init --fix-gitignore` then writes `.gitignore`, `.goalrail/` and `openspec/` alongside `HEAD`, `objects` and `refs`, while still refusing to register. | `git init --bare`; `gr init --fix-gitignore --repo <bare>` | 2026-07-31T12:53:00Z | Pre-existing, and the new writer inherits it unless the ignore path gates on a work tree. One condition fixes both. |
| CTX-14 | repository | The existing writer already guards the corruption an append introduces — `internal/ambient/scope.go:404-407` supplies a missing final newline. Without that guard, appending to a file whose last hand-written line has no newline concatenates the two, leaving neither path ignored. | `internal/ambient/scope.go:404-407`; a naive `>>` reproduction leaving both `TODO.md` and the registration path unignored | 2026-07-31T12:51:00Z | The new writer reuses an existing guard rather than inventing one. Reproduced because the failure is silent in both directions. |
| CTX-15 | external | The per-clone exclude does not travel. A working tree shipped without its `.git` — `tar --exclude=.git`, `docker COPY` — lands in a fresh clone with the registration file stageable, which is the harm the refusal exists to prevent. A plain re-clone is safe by contrast: the file was never committed, so it is simply absent. | Reproduced end to end: tar of a registered tree extracted over a fresh clone, then `git add -A` → `A .claude/settings.local.json` | 2026-07-31T12:54:00Z | The honest cost of the per-clone location, and the one case where the tracked entry is genuinely stronger. |
| CTX-16 | repository | In a directory that is not yet a repository, `gr init` registers with no notice at all, and a later `git init` makes the registration stageable. Neither the current design nor the proposed one addresses it: the ignore read answers "ignored" for a non-repository (`scope.go:343-345`) and the writer then skips every entry (`scope.go:393-396`). | `gr init` in a plain directory → `applied=true`, no notices; then `git init && git add -A` → the file stages | 2026-07-31T12:53:00Z | A silent instance of the same harm the visible refusal exists to prevent. Adjacent to this change and cheap to close, but it is a separate defect. |
| CTX-17 | repository | Linked worktrees share one exclude file in the common directory; there is no per-worktree `info/`. One initialization therefore covers every sibling worktree. | `git worktree add`, then `rev-parse --git-common-dir` from each | 2026-07-31T12:50:00Z | Convenient rather than harmful, but it means the write is not scoped to the worktree it was run in, and the report should not imply that it is. |
| CTX-18 | repository | Registering the first scaffold at user scope instead — which would dissolve the problem rather than route around it — is excluded by a confirmed non-goal, `openspec/changes/archive/2026-07-30-harness-init-v0/intent.md` NG-6, and by the promoted text at `openspec/specs/ambient-connect/spec.md:79-104`. | Both files read directly | 2026-07-31T12:58:00Z | Closes the most attractive-looking alternative without relitigating a settled decision. |
| CTX-19 | repository | Every behaviour change in this repository ships through the full cycle, including single-defect fixes: the archive holds nineteen change directories, among them `repair-codex-hook-linkage-v0` and `repair-stale-executable-v0`. | `openspec/changes/archive/` | 2026-07-31T12:25:00Z | Sets the artifact set this change owes, and rules out treating it as a patch. |
| CTX-20 | repository | The diagnosis already reports whether the attachment is registered, among its named states. | `openspec/specs/harness-doctor/spec.md:17-21` | 2026-07-31T13:02:00Z | A re-clone self-heals — the registration is absent too, so initialization runs again and rewrites the exclude. Only the tree-shipped-without-`.git` case of CTX-15 leaves a registration the diagnosis will call healthy while it has become shareable. |

## Material Unknowns

None.
