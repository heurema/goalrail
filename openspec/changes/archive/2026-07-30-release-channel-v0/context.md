# Context Pack

- **Context Pack ID:** context-release-channel-v0
- **Version:** 2
- **Previous version:** 1
- **Started at:** 2026-07-30T15:05:00Z
- **Completed at:** 2026-07-30T15:59:30Z
- **Outcome:** sufficient

Version 2 extends version 1 after an owner-directed critical review of candidate
intent version 1. Every version 1 item stands as recorded; CTX-16 through CTX-25
add facts the review surfaced and this session reproduced, several of which
correct an inference the candidate drew rather than a claim the pack made.

## Context Items

| ID | Kind | Claim | Source | Observed at | Relevance |
|---|---|---|---|---|---|
| CTX-1 | repository | The repository has no `.github` directory at all: no continuous integration, no workflow, no release pipeline. Nothing is verified between a local `go test` and a merge. | repo:root listing at `3beb72c` | 2026-07-30T15:06:00Z | This change creates the first automated check the project has, so there is no existing workflow contract to preserve or extend. |
| CTX-2 | repository | There are zero tags, so `go install github.com/heurema/goalrail/cmd/gr@latest` resolves to the default branch's newest commit today. Once a release tag exists, `@latest` resolves to that tag instead. | repo:`git tag`; local:`go help install`, `go help module-get` | 2026-07-30T15:06:00Z | The first tag silently changes what the README's documented install command gives a user: the branch tip today, a fixed release afterwards. |
| CTX-3 | repository | `harness.Version` is the constant `"0.1.0"` at `internal/harness/doctor.go:19`. It is reported by `gr version` beside the canon ID and by `gr doctor` in both its JSON and its report header, and the promoted requirement states the version is information about the binary from which no verdict about a repository may be derived. | repo:internal/harness/doctor.go, cmd/gr/harness.go, openspec/specs/harness-doctor/spec.md | 2026-07-30T15:08:00Z | Fixes exactly what changes when the version stops being a constant, and confirms nothing about a repository's state depends on the change. |
| CTX-4 | repository | Go itself already stamps a version into every build, verified in a scratch clone with a throwaway local tag: clean checkout exactly at tag `v0.1.0` — `v0.1.0`; one commit after the tag — `v0.1.1-0.<timestamp>-<commit>`; no tags at all — `v0.0.0-<timestamp>-<commit>`; dirty working tree — the same with `+dirty`; `-buildvcs=false` or no VCS — `(devel)`. A `--depth 1 --branch v0.1.0` clone still stamps `v0.1.0`. | local:`go build` + `go version -m` across five build situations, scratch clone of this repository | 2026-07-30T15:18:00Z | Corrects the brief's first tripwire. A checkout build is not versionless: it carries a commit-derived version, and `(devel)` appears only when VCS stamping is off. It also shows the tag reaches a shallow CI checkout, so build information alone can carry the release version. |
| CTX-5 | repository | `darwin/arm64`, `darwin/amd64`, `linux/amd64` and `linux/arm64` all build with `CGO_ENABLED=0`. `windows/amd64` does not: `internal/boundedio/bounded_file.go:27:53: undefined: syscall.O_NOFOLLOW`. | local:`CGO_ENABLED=0 GOOS=… GOARCH=… go build -o /dev/null ./cmd/gr`, five platforms at `3beb72c` | 2026-07-30T15:10:00Z | Fixes the exact build matrix this change can claim, and reconfirms the Windows gap at current `main` rather than at the commit the brief checked. |
| CTX-6 | repository | The flags that fail on Windows are how a promoted guarantee is implemented: `boundedio` opens the escalation artifact with `O_NOFOLLOW|O_NONBLOCK`, and `ambient-connect` requires that a stale artifact which is oversized, non-regular, or a link outside the repository is cleared and noted without being followed. | repo:internal/boundedio/bounded_file.go, openspec/specs/ambient-connect/spec.md | 2026-07-30T15:11:00Z | Explains why the Windows gap is a decision with a recorded reason rather than a missing matrix entry: a build-tagged stub would compile and drop a promoted guarantee silently. |
| CTX-7 | repository | Verification here is the Go toolchain and nothing else: `go vet ./...` is clean and `go test ./...` is green across 13 packages in about 12 seconds of wall clock. There is no Makefile, lint configuration, or task runner, and `go.mod` declares no dependency at all, so a build needs no module download. | local:`go vet ./...`, `go test ./... -count=1` at `3beb72c`; repo:go.mod, root listing | 2026-07-30T15:12:00Z | Establishes the pre-change baseline and shows what continuous integration has to run — and that it is cheap, hermetic, and needs no dependency cache. |
| CTX-8 | repository | `TestPinnedCLIAcceptsTheEmbeddedCanon` is the only development-side check that the embedded canon is one the stock CLI accepts. It skips — loudly, never fails — under `-short`, with no `npx` on the path, or when the pinned package is absent from the npm cache, because it probes with `--offline` so an ordinary `go test` never reaches the network. It passes locally here, where the cache is primed. | repo:internal/harness/canon_pinned_cli_test.go; local:`go test ./internal/harness -run TestPinnedCLIAcceptsTheEmbeddedCanon -v` | 2026-07-30T15:14:00Z | Confirms the brief's second tripwire at the source: on a fresh runner with an empty cache the check would skip and the run would still be green, so the canon would ship unverified unless the workflow primes the cache and treats a skip as a failure. |
| CTX-9 | repository | The promoted `harness-update` requirement forbids a network lookup for a newer release and justifies the prohibition by the channel's absence: "There is no release channel to query, so the command MUST NOT attempt a network lookup for a newer release." | repo:openspec/specs/harness-update/spec.md | 2026-07-30T15:15:00Z | This change ends the stated reason while the prohibition itself must survive, so the promoted text becomes false unless it is restated in the same change. |
| CTX-10 | repository | Confirmed non-goal NG-1 of the archived `harness-init-v0` reads: no network release check and no self-update of the `gr` binary, because there is no tag, no release pipeline, and no channel to query — "Revisit when a release channel exists." | repo:openspec/changes/archive/2026-07-30-harness-init-v0/intent.md | 2026-07-30T15:15:00Z | Names the recorded revisit condition this change satisfies, and the boundary it does not cross: creating the channel is not querying it. |
| CTX-11 | external | GitHub Actions is enabled for the repository with `allowed_actions: all` and SHA pinning not required. The repository is public, MIT-licensed, its default branch is `main`, and it has no releases. | external:`gh api repos/heurema/goalrail/actions/permissions`, `gh api repos/heurema/goalrail` (read-only) | 2026-07-30T15:16:00Z | Confirms a workflow can run and publish at all, that third-party actions are permitted, and that public-repository runner minutes carry no cost gate for a macOS job. |
| CTX-12 | repository | The README documents `go install …@latest` as the only install route, repeats it inside the prompt a user is told to paste to their agent, states under Status that no release binaries are published, and lists a `gr version` command. The repository is MIT-licensed, and the licence requires its notice to accompany distributed copies. | repo:README.md, LICENSE | 2026-07-30T15:17:00Z | Fixes every place the documented install path has to change, and gives the reason a published archive carries the licence text beside the binary. |
| CTX-13 | repository | `go.mod` declares `go 1.26.0`; the toolchain building it locally is `go1.26.1`. | repo:go.mod; local:`go version -m` output header | 2026-07-30T15:18:00Z | The workflow must resolve a toolchain from the repository rather than pinning a version string in YAML that drifts from `go.mod`. |
| CTX-14 | owner | The brief records three decisions and two boundaries: a workflow of our own rather than goreleaser, revisited if a tap or signing is wanted; Windows unsupported with the reason recorded rather than the gap left silent, revisited on a real user; the full OpenSpec cycle kept to a narrow slice. Creating the first tag and publishing a release each keep their own owner gate, and the deferred branch `codex/dogfood-run-v0` and the owner's scaffold configuration stay untouched. | repo:docs/release-channel-brief.md | 2026-07-30T15:20:00Z | These are the owner's decisions carried into this change; the candidate intent restates them as boundaries rather than re-deciding them. |
| CTX-15 | owner | The brief states that a user who downloads an unsigned macOS binary through a browser meets a Gatekeeper warning that `go install` never produces, and that the install documentation should say so rather than let the user discover it. | repo:docs/release-channel-brief.md | 2026-07-30T15:20:00Z | A documented consequence of the "no signing" decision, recorded as an owner claim rather than something verified here; it shapes what the install documentation must say. |
| CTX-16 | repository | Any untracked, non-ignored file anywhere in the work tree marks the build dirty, including a build's own output. Reproduced at a tagged checkout: the first build into `./dist` stamps `v0.1.0`; every later build stamps `v0.1.0+dirty` — including one written outside the tree, because the earlier artifact is still inside it — and removing the directory restores the clean stamp. `.gitignore` holds no build-output entry. | local:`go build` into `./dist` at tag `v0.1.0` in a scratch clone, `go version -m`; repo:.gitignore | 2026-07-30T15:50:00Z | A workflow that writes four binaries and a checksums file into the checkout stamps three of them `+dirty` and fails its own release gate. Writing artifacts outside the tree is not by itself sufficient: what matters is that nothing untracked exists in the tree while a build runs. |
| CTX-17 | repository | A `go test` binary reports `(devel)` in every Git state, so no in-process test can observe a stamped version. `go version -m` reads the stamp out of any `GOOS`/`GOARCH` binary without executing it. A dirty tree exactly at a tag stamps `v0.1.0+dirty`, and a tag-following fetch that pulls a second tag on the same commit stamps the higher tag, while a single-ref `--no-tags` fetch stamps the requested one. | local:scratch clone, `go test` reading `debug.ReadBuildInfo`, `go version -m` on a `CGO_ENABLED=0 linux/amd64` binary from a darwin host, two tags on one commit | 2026-07-30T15:51:00Z | Fixes what can actually verify the version: a unit test over injected build information, and a release gate that reads each artifact's stamp without running it and compares it to the tag with strict equality. |
| CTX-18 | external | `curl` sets no `com.apple.quarantine` — only `com.apple.provenance` — and `/usr/bin/tar` does not carry a quarantine attribute from an archive onto the extracted file, so a binary fetched and unpacked from the command line runs with no prompt. `codesign -dv` on a fresh `CGO_ENABLED=0` darwin/arm64 build reports `adhoc, linker-signed`; darwin/amd64 is not signed at all. Apple removed the Control-click "Open" override for non-notarized software in macOS Sequoia. | local:macOS 26.5.2, `curl`, `xattr`, `/usr/bin/tar`, `codesign -dv` on this repository's own build; external:Apple platform release notes for Sequoia | 2026-07-30T15:52:00Z | The Gatekeeper warning belongs to the browser-and-Finder path, not to the documented command-line route, so an unqualified warning would be false for the flow the change exists to enable. It also fixes the correct remedy and the correct word: "not notarized" rather than "unsigned", and makes post-link modification of the arm64 binary unsafe. |
| CTX-19 | repository | A registration names the running executable's symlink-resolved absolute path, and the diagnosis fails when nothing runnable is at that path: `currentExecutable` resolves `os.Executable` through `filepath.EvalSymlinks`, `managedCommand` quotes that path into the hook, and health reports "the registered Goalrail executable is missing at …". | repo:cmd/gr/ambient.go:307, internal/ambient/connect.go:524, internal/ambient/health.go:339 | 2026-07-30T15:55:00Z | An agent that runs `gr init` from a temporary download directory produces an attachment that breaks as soon as the directory is cleaned. The download instructions must name a durable location, and the paste-prompt's "do not edit any file outside this repository" has to make room for it. |
| CTX-20 | repository | `shasum -a 256 -c checksums.txt` exits `1` with "FAILED open or read" when the file lists archives that were not downloaded; `--ignore-missing` exits `0`. The GNU equivalent is `sha256sum -c --ignore-missing`. | local:scratch directory with one of two listed files present | 2026-07-30T15:56:00Z | A user or agent downloading one archive out of four and running the obvious command gets a failure, so the documented verification command has to be the `--ignore-missing` form. |
| CTX-21 | external | `https://github.com/<owner>/<repo>/releases/latest/download/<asset>` redirects to that asset in the newest release — probed live against a public repository, returning `302` to the versioned asset path. The name in the URL must match exactly, so an asset whose name carries the version has no stable URL while a version-free name does. | external:`curl -sI https://github.com/cli/cli/releases/latest/download/checksums.txt` (read-only, public) | 2026-07-30T15:57:00Z | Under version-bearing archive names, the checksums file is the only asset with a stable address, which makes it the discovery entry point: fetch it, read the current names, then fetch the one archive needed. |
| CTX-22 | external | `gh release create` with assets creates the release as a draft, uploads the assets, and publishes only afterwards; its own help states this, and it also states that immutability protections apply once a release is published. `gh` is preinstalled on GitHub-hosted runners. | local:`gh --version` (2.92.0), `gh release create --help` | 2026-07-30T15:58:00Z | All-or-nothing publishing is the default behaviour of the obvious tool rather than something to engineer, and a failed upload leaves an unpublished draft. It also fixes the recovery path: a published release is not repaired in place, so a botched one is superseded by a new tag. |
| CTX-23 | repository | `go test` exits `0` and prints `ok` when the pinned-CLI check skips, while `go test -json` emits `"Action":"skip"` for that test in the same run. | local:`go test -short -run TestPinnedCLIAcceptsTheEmbeddedCanon ./internal/harness`, with and without `-json` | 2026-07-30T15:58:30Z | A workflow can fail on a skipped canon check by reading the machine-readable stream, with no change to the test and no new dependency, so the guarantee needs no escape hatch inside the harness. |
| CTX-24 | external | `main` has no branch protection and no rulesets, and the repository's default workflow permission is `write`. | external:`gh api repos/heurema/goalrail/branches/main/protection` (404 not protected), `.../rulesets` (empty), `.../actions/permissions/workflow` (read-only) | 2026-07-30T15:53:00Z | What continuous integration can promise here is a visible failed check, not a blocked merge; and because the token is broadly privileged by default, each workflow should declare the narrow permission it needs rather than inherit that default. |
| CTX-25 | repository | Archived changes are history: a material amendment creates a new version and preserves the prior one, and corrections are appended as new records rather than written over prior evidence. | repo:docs/project-lifecycle.md, AGENTS.md, openspec/specs/intent-snapshot/spec.md | 2026-07-30T15:59:00Z | A revisit condition recorded in an archived change cannot be marked satisfied by editing the archive. The satisfaction is recorded in this change's own artifacts and in the promoted requirement that carried the reasoning. |

## Material Unknowns

None.

The three open questions the brief leaves — which version the first tag carries,
whether the proposed artifact naming is confirmed, and whether continuous
integration runs on pull requests as well as on the tag — are owner decisions,
not evidence gaps. No further collection can answer them, so they are carried
into the candidate Intent Snapshot as recorded ambiguities with a proposed answer
each, to be resolved by owner confirmation rather than by inference.
