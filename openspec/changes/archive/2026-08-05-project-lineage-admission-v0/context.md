# Context Pack

- **Context Pack ID:** ctx-project-lineage-admission-v0
- **Version:** 1
- **Previous version:** pending
- **Artifact Contract:** goalrail-context-intent
- **Artifact Contract Version:** 1
- **Started at:** 2026-08-04T16:57:38Z
- **Completed at:** 2026-08-04T16:59:49Z
- **Outcome:** sufficient

## Context Items

| ID | Kind | Claim | Source | Verification recipe | Observed at | Relevance |
|---|---|---|---|---|---|---|
| CTX-1 | repository | In a live Deltacue observation, the primary checkout and a linked Codex worktree shared commit `4df20fdd140933c771a8db814804cb60077a7e70` and one Git common directory, while Goalrail v0.1.8 reported `initialized=true, working=true` only in the primary checkout and `initialized=false, working=false` in the linked worktree. | task:deltacue-linked-worktree-reproduction | Prerequisite: retain both observed checkouts. In each, read `git rev-parse HEAD`, `git rev-parse --git-common-dir`, and `GR_NO_UPDATE_CHECK=1 gr doctor --json --scaffold codex`; expect one commit and common directory but different initialization and working states. | 2026-08-04T16:20:00Z | This is the concrete failure that exposed Goalrail project identity being modeled as checkout-local state. |
| CTX-2 | repository | Initialization writes `.goalrail/ambient.json` under the current worktree root, and initialization checks read only that worktree-local path. The archived design deliberately required every worktree to initialize independently. | repo:internal/ambient/ambient.go | Read the named functions and design section; expect marker lookup to join the marker path to the supplied worktree root and the archived decision to reject inherited attachment between worktrees. | 2026-08-04T16:21:00Z | A marker-location patch would repair only one symptom and preserve the rejected local opt-in model. |
| CTX-3 | repository | At the observed Deltacue commit, tracked Goalrail initialization output consists of the OpenSpec configuration and the `goalrail-intent` schema/templates; there is no committed Goalrail project declaration, `AGENTS.md`, or `CLAUDE.md`. | git:4df20fdd140933c771a8db814804cb60077a7e70 | At the retained commit, read `git ls-files AGENTS.md CLAUDE.md '.goalrail*' 'openspec/**'`; expect only the OpenSpec overlay paths and no committed project declaration or supported-agent bootstrap instruction. | 2026-08-04T16:59:49Z | A clean clone receives planning files but no unambiguous statement that Goalrail governs all material project work. |
| CTX-4 | repository | The current CLI says `init -> work normally; ordinary work needs no Goalrail command`. Its ambient hook is fail-quiet and only announces or retains session information; it neither mediates arbitrary code writes nor performs project admission. | repo:cmd/gr/main.go | Read the named dispatch and hook paths, then read `GR_NO_UPDATE_CHECK=1 gr --help`; expect no project-wide lineage verifier or admission command and expect ordinary-work guidance. | 2026-08-04T16:33:00Z | The requested guarantee does not exist even in an initialized checkout, so worktree discovery alone cannot satisfy the owner. |
| CTX-5 | repository | Current ambient specifications require uninitialized or unrelated sessions to produce zero observation and zero Goalrail-owned writes. | repo:openspec/specs/ambient-connect/spec.md | Read the named requirement; expect an initialization check before observation and inert behavior outside a connected scope. | 2026-08-04T16:25:00Z | A repository-carried declaration must make managed projects discoverable without causing Goalrail to observe unrelated repositories. |
| CTX-6 | repository | Goalrail already has provider-neutral building blocks for a lineage chain: a frozen WorkSpec binds a confirmed intent, repository revision, scope, checks, and stop conditions; a run binds the WorkSpec digest to provider-authoritative session identity; and a bounded terminal receipt carries intent, run, check, status, and delta references. | repo:openspec/specs/work-spec/spec.md | Read the named requirements; expect explicit intent-to-WorkSpec, WorkSpec-to-run/session, and run-to-terminal-receipt bindings without raw prompts or provider-specific canonical fields. | 2026-08-04T16:48:00Z | The new project admission chain should reference and extend existing canonical artifacts rather than invent a duplicate intent or receipt source. |
| CTX-7 | repository | Goalrail has a precedent for committable repository guidance in `.goalrail-review.md`, but that guidance is materialized only for independent review and does not govern all development work. | repo:internal/review/instructions.go | Read the named constant and materialization function; expect a committable review instruction file separate from ambient initialization and general admission. | 2026-08-04T16:33:00Z | Thin committed bootstrap surfaces are feasible, but the existing review file is too narrow to be the project-governance declaration. |
| CTX-8 | external | The owner corrected the product boundary: once Goalrail initializes a project, Goalrail becomes a durable project invariant across clones, worktrees, developers, and tools. Material code work must follow the same Goalrail intent and current OpenSpec flow; another tool may execute the work but may not establish a parallel intent authority and still count as conformant project work. | task:owner-product-boundary | Not independently reproducible — this is the owner's product decision; retained evidence: the task that superseded `intent-repository-wide-ambient-init` version 2. | 2026-08-04T16:35:00Z | This sets the new source of truth and broadens the change from ambient initialization to project governance and admission. |
| CTX-9 | external | The owner requires each material work unit to remain reconstructable as one connected chain: project policy, intent, change, provider sessions/runs, commits, pull request, comments/reviews, checks, terminal receipt, and owner decision. An unlinked material change is a defect. Legitimate exceptions may exist only if the design makes them explicit and deterministic. | task:owner-traceability-requirement | Not independently reproducible — this is the owner's traceability requirement; retained evidence: the initiating task discussion preceding this change. | 2026-08-04T16:40:00Z | This defines the admission evidence and prevents hooks or a marker from being mistaken for the outcome. |
| CTX-10 | external | One owner-authorized critique recommended a deterministic verifier at local and protected integration boundaries, local hooks only as early feedback, backward references for late identifiers, and categorical results that never collapse exceptions into valid. | receipt:independent-architecture-critique-2026-08-04 | Not independently reproducible — the one-shot external critique is historical evidence and rerunning could produce different text; retained evidence: the model-router receipt in the initiating task. | 2026-08-04T16:47:00Z | This closes the local-hook loophole and provides a reversible admission model without prematurely requiring a controlled write gateway. |
| CTX-11 | repository | The current marker, scaffold attachment, user-local binary, Git configuration, and ordinary clone-local hook directory are not tracked project content; only tracked project instructions and declarations are guaranteed to arrive with a normal clone. | task:deltacue-tracked-files-inspection | In the retained clone, read and compare `git ls-files`, `.git/info/exclude`, `git config --show-origin --get core.hooksPath`, and `git rev-parse --git-path hooks`; expect the current marker and local attachment outside the tracked project file set. | 2026-08-04T16:33:00Z | Clean-machine discovery requires committed bootstrap content, while local enforcement components still need an explicit setup operation. |
| CTX-12 | external | The owner selected the clean-machine experience: when a supported agent receives a feature request in a managed clone without Goalrail prerequisites, it performs a read-only preflight, writes no code, explains the exact pinned setup bundle and changes, asks permission, installs the approved known components itself, verifies health, emits a setup receipt, and automatically returns to the original request at the intent gate. A newly discovered dependency is a separate permission request. | task:owner-clean-machine-experience | Not independently reproducible — this is an owner-confirmed experience decision; retained evidence: the owner's explicit acceptance immediately before this change was created. | 2026-08-04T16:55:00Z | This turns a fail-closed clone into a friendly recovery path without converting a feature request into silent installation authority. |
| CTX-13 | repository | Project rules treat installation, authentication, credential changes, and user- or system-level configuration as separate enabling actions. Approval must state exactly what changes, why existing paths are insufficient, and how to undo it; unknown enabling actions cannot be bundled into outcome approval. | repo:AGENTS.md | Read the named sections; expect explicit consent boundaries for installation and other enabling actions, plus read-only preflight before requesting one state-changing action. | 2026-08-04T16:56:00Z | The setup UX must enumerate a known reversible bundle and may not silently add Node, OpenSpec, credentials, or global configuration. |
| CTX-14 | repository | Goalrail intent is upstream, OpenSpec is a replaceable compiler/provider, and canonical Goalrail semantics must remain provider-neutral. A model-produced intent is candidate until the owner actively verifies its exact version. | repo:AGENTS.md | Read the named project rules and config; expect owner-confirmed versioned intent before proposal, plus an explicit prohibition on leaking OpenSpec or provider types into the canonical domain. | 2026-08-04T16:57:00Z | The change must reference existing canonical artifacts, avoid a second intent source, and pause for exact owner confirmation before proposal or implementation. |

## Material Unknowns

None.
