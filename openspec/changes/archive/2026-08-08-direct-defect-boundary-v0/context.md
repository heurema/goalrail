# Context Pack

- **Context Pack ID:** direct-defect-boundary-v0
- **Version:** 2
- **Previous version:** 1
- **Artifact Contract:** goalrail-context-intent
- **Artifact Contract Version:** 1
- **Started at:** 2026-08-07T17:00:00Z
- **Completed at:** 2026-08-07T18:05:00Z
- **Outcome:** sufficient

Version 2 adds CTX-9 through CTX-14, gathered from two independent critiques of
the version 1 proposal. They establish that the mechanism this repository would
have to duplicate already exists and is already owner-gated, that it classifies
paths rather than the ordering the proposal is about, and that this repository
cannot presently adopt it. Version 1 items are preserved unchanged.

## Context Items

| ID | Kind | Claim | Source | Verification recipe | Observed at | Relevance |
|---|---|---|---|---|---|---|
| CTX-1 | repository | The obligation to write an Intent Snapshot is triggered by one undefined word. `AGENTS.md` requires one "for every significant change" and the term is defined nowhere in the repository. | `AGENTS.md:6` | `grep -rn "significant" AGENTS.md openspec/specs/`; expect two occurrences, one stating the obligation and one naming it in a scenario, and no definition. | 2026-08-07T17:04:00Z | The trigger for the whole planning cycle rests on a word each author interprets about their own work. |
| CTX-2 | repository | No defect-class exception exists. Nothing in the contract or the specifications says when a change may be made directly rather than through the cycle. | `AGENTS.md`; `openspec/specs/` | `grep -rniE "defect\|direct change\|bypass\|exempt" AGENTS.md openspec/specs/`; expect matches about health-report repair, lineage exemption classes, and artifact order, and none about when a change may skip the cycle. | 2026-08-07T17:05:00Z | The exemption authors already rely on is not written down, so it cannot be cited, reviewed, or disputed. |
| CTX-3 | repository | The specification governing Intent Snapshots covers how one is confirmed, versioned, traced and bounded, but never when one is required. Every requirement presumes a snapshot already exists. | `openspec/specs/intent-snapshot/spec.md` | Read the nine requirements; expect confirmation gating, provenance, versioning and authority limits, and no statement of the entry condition. | 2026-08-07T17:06:00Z | The entry condition is the one part of the workflow that lives only in prose, which is why it is the part that failed. |
| CTX-4 | repository | The absence has already cost a P1 in review. `#85` was carried out as a direct change and an independent review required the missing snapshot. | `https://github.com/heurema/goalrail/pull/85#discussion_r3736621429` | Read the review comment titled "Add the required versioned Intent Snapshot"; expect a P1 badge. | 2026-08-07T17:08:00Z | The boundary was decided by the author and found wrong afterwards, which is the failure this change addresses. |
| CTX-5 | repository | The recovery from that P1 is recorded in the archive and states its own limits: the snapshot was composed after the implementation, is not evidence of intent that preceded it, and cannot become so by being confirmed. | `openspec/changes/archive/2026-08-07-pin-closure-record-v0/intent.md` | Read the Provenance section; expect it to say the work was carried out as a direct change and that review flagged the missing snapshot twice. | 2026-08-07T17:09:00Z | Shows what the absence produces in practice — a retrospective artifact that is honest but cannot supply what it is standing in for. |
| CTX-6 | repository | That same record names the gap explicitly as unanswered: whether a retrospective confirmation settles a procedural departure is "a question the governing contract does not answer". | `openspec/changes/archive/2026-08-07-pin-closure-record-v0/intent.md` | Read the final sentence of the Provenance section. | 2026-08-07T17:10:00Z | The repository has already identified this hole in its own words; this change closes the entry condition rather than the settlement question. |
| CTX-7 | repository | The workspace contract requires that a promotion escalate when its class recurs, and records that only promotions landing as enforced artifacts stopped their class; prose alone did not. | `/Users/vi/personal/heurema/AGENTS.md` | Read the ratchet section; expect the reliability ladder from reviewer prose to schema requirement to code with a test, and the 2026-08-01 measurement. | 2026-08-07T17:12:00Z | Constrains the form of the answer: a rule stated only as advice is the rung already measured as insufficient. |
| CTX-8 | repository | The lifecycle document already owns the entry point of a change, describing the artifact order an operator follows when a significant change begins. | `openspec/specs/context-pack/spec.md:62` | Read the scenario "New change begins". | 2026-08-07T17:13:00Z | Identifies where an entry condition belongs and confirms it is currently stated without the criterion that would decide it. |
| CTX-9 | repository | An instruction surface is forbidden from defining a second materiality policy or selecting a parallel spec authority, and materiality is already owned deterministically elsewhere. | `openspec/specs/project-governance/spec.md:51`; `openspec/specs/lineage-admission/spec.md:7` | Read the paragraph beginning "Agent-specific instruction surfaces"; then read the policy requirement it defers to. | 2026-08-07T17:45:00Z | A criterion for what counts as significant, written into the contract, would be exactly the second policy this forbids. |
| CTX-10 | repository | A mechanical gate on unconfirmed intent already exists in specification and in code: candidate or malformed intent MUST NOT satisfy a required relation, material paths lacking lineage classify `MISSING`, and `MISSING` always denies admission. | `openspec/specs/lineage-admission/spec.md:39,41`; `internal/admission/evaluate.go`; `internal/admissiongit/` | Read both spec paragraphs; then read the evaluator and the Git range collector. | 2026-08-07T17:50:00Z | The part of the problem that is mechanically decidable is already solved, so a new rule must not restate it. |
| CTX-11 | repository | That verifier decides paths, not order. It classifies by path, change kind, priority and policy rule, and knows nothing of an author naming a requirement before implementation. | `internal/admission/evaluate.go` | `grep -rniE "direct.defect\|restoration.claim\|named requirement\|before implementation" internal/admission internal/admissiongit internal/domain internal/lineage`; expect no matches. | 2026-08-07T17:55:00Z | Establishes the residual gap: the ordering question this change exists for is outside the existing mechanism's model. |
| CTX-12 | repository | The ordering failure this change addresses survives the existing mechanism. In `#85` the implementation was committed at 14:25, the retrospective snapshot at 15:51, and confirmation at 16:10; after that backfill the verifier sees a confirmed snapshot bound to the work unit and no violation. | `https://github.com/heurema/goalrail/pull/85`; `openspec/changes/archive/2026-08-07-pin-closure-record-v0/intent.md` | Read the pull request's commit timestamps; then read the Provenance section stating the snapshot was composed after the implementation. | 2026-08-07T18:00:00Z | Shows the gap is not theoretical: the one recorded instance would pass a fully active verifier. |
| CTX-13 | repository | This repository cannot presently carry the committed declaration the governance contract requires: `.goalrail/` is ignored in its entirety and holds no tracked file. | `.gitignore:4`; `6c1f022` | `git check-ignore -v .goalrail/project.json` and `git ls-files .goalrail | wc -l`; expect the ignore rule and zero. | 2026-08-07T18:02:00Z | Bounds what this change can deliver: a check that depends on self-adoption cannot be turned on here today. Recorded separately as issue #89. |
| CTX-14 | repository | Activation of shared admission is deliberately staged behind an owner-authorized external action, and the workflow that would carry it has no production caller. | `openspec/specs/lineage-admission/spec.md:88`; `internal/project/canon/prepared-admission.yml`; `internal/githubadmission/workflow.go:24` | Read the requirement forbidding an unverified active claim; read the canon entry marked `prepared`; then `grep -rn "RenderWorkflow(" --include='*.go' . | grep -v _test.go` and expect only the definition. | 2026-08-07T18:04:00Z | Confirms that enabling the mechanism is not this change's to perform, and must not be assumed by it. |

## Material Unknowns

None.
