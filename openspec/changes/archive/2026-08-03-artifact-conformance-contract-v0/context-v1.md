# Context Pack

- **Context Pack ID:** CTXP-artifact-conformance-contract-v0
- **Version:** 1
- **Previous version:** pending
- **Started at:** 2026-08-02T18:16:34Z
- **Completed at:** 2026-08-02T18:19:21Z
- **Outcome:** sufficient

## Context Items

| ID | Kind | Claim | Source | Verification recipe | Observed at | Relevance |
|---|---|---|---|---|---|---|
| CTX-1 | repository | The implementation basis is the `canon-template-gaps` branch at `8fd903e`; this new change is the only working-tree addition observed after change creation. | `git:canon-template-gaps@8fd903e` | In `/Users/vi/personal/heurema/goalrail`, run `git branch --show-current`, `git rev-parse HEAD`, and `git status --short`; expect `canon-template-gaps`, `8fd903e...`, and only `openspec/changes/artifact-conformance-contract-v0/` as new work. | 2026-08-02T18:16:34Z | The contract must be designed and tested against the actual #56 result, not the older clean worktree on `main`. |
| CTX-2 | repository | The production parser requires Context Pack metadata shaped exactly as `<id> version <number>`, while the confirmed #56 intent declares `CTXP-canon-template-gaps-v0 v2 (...)`; the artifact and reader therefore disagree. | `git:8fd903e:internal/adapters/openspec/adapter.go#L374-L385`; `git:8fd903e:openspec/changes/canon-template-gaps-v0/intent.md#L3-L8` | Run `git show 8fd903e:internal/adapters/openspec/adapter.go` and inspect `parseContextPackDeclaration`, then run `git show 8fd903e:openspec/changes/canon-template-gaps-v0/intent.md`; expect the exact `version` grammar in code and the `v2` declaration in the artifact. | 2026-08-02T18:16:34Z | This is the surviving format failure the owner finds unreasonable; a contract must make accepted forms deliberate and diagnostics actionable. |
| CTX-3 | repository | The adapter canon test parses `context.md` and `intent.md` independently, so it never exercises cross-file identity/version binding; the local-run production resolver loads the sibling context and does exercise that binding. The adapter suite still passes. | `git:8fd903e:internal/adapters/openspec/adapter_canon_test.go#L12-L85`; `git:8fd903e:internal/adapters/openspec/adapter.go#L237-L241`; `git:8fd903e:internal/adapters/openspec/work_spec_intent.go#L52-L104` | Run `go test ./internal/adapters/openspec/...`; expect green. Inspect the cited calls; expect `ReadIntent` to pass no Context Pack while `readResolvedIntent` passes the parsed sibling pack to `readIntent`. | 2026-08-02T18:19:21Z | A green test through test-only wiring is not evidence that the production composition accepts an artifact. |
| CTX-4 | repository | Ambient discovery is a second production composition: it calls isolated `ReadIntent` and silently skips unreadable or malformed active intents. Its suite also passes. | `git:8fd903e:internal/ambient/intent.go#L45-L90` | Run `go test ./internal/ambient/...`; expect green. Inspect lines 63-73; expect read and parse errors to take an unreported `continue` path. | 2026-08-02T18:19:21Z | Conformance must cover local-run, development compiler, and ambient behavior separately; aggregate discovery cannot mask a broken active artifact. |
| CTX-5 | repository | `LoadChange` performs context/intent/proposal compilation but has no production caller, so it cannot stand in for either production composition root. | `git:8fd903e:internal/adapters/openspec/adapter.go#L96-L162`; codebase graph for `LoadChange` inbound calls | Run a graph inbound trace for `internal.adapters.openspec.adapter.LoadChange`, excluding tests; expect no callers. As a local fallback, run `rg -n 'LoadChange\\(' --glob '*.go'`; expect the definition and test references only. | 2026-08-02T18:16:34Z | The development compiler deserves its own conformance lane, but passing it cannot prove `gr prepare` or ambient discovery. |
| CTX-6 | repository | The seven-column Context Pack reader accepts a `Verification recipe` cell but discards it: `domain.ContextItem` has no recipe field and `parseContextItems` never stores column 4. | `git:8fd903e:internal/adapters/openspec/adapter.go#L584-L620`; `git:8fd903e:internal/domain/types.go#L53-L62` | Inspect both cited definitions; expect the parser to read the seven-column row, assign source, observed time, and relevance, but assign no recipe to the domain value. | 2026-08-02T18:16:34Z | Contract conformance must detect silent semantic loss, not merely successful parsing. |
| CTX-7 | repository | `harness.Update` classifies the overlay, then backs up and materializes without locking the repository or revalidating immediately before the first overwrite; a concurrent edit can therefore invalidate the classification used to authorize replacement. | `git:8fd903e:internal/harness/update.go#L78-L161`; `git:8fd903e:internal/harness/overlay.go#L272-L333` | Inspect the cited call sequence; expect `InspectOverlay`, then backup, then `Materialize(..., true)` with no repository lock or compare-before-write guard between inspection and overwrite. | 2026-08-02T18:16:34Z | A historical negative control for the #56 update race needs a safe write contract, not only a parser fixture. |
| CTX-8 | repository | One external-review allegation was false: `TestASupersededFileIsReportedNotDeleted` restores `previousCanons` with `defer`, and twenty shuffled harness runs pass. | `git:8fd903e:internal/harness/update_test.go#L229-L231`; local test observation | Run `go test -shuffle=on -count=20 ./internal/harness`; expect green. Inspect lines 229-231; expect save, replacement, and deferred restoration of `previousCanons`. | 2026-08-02T18:19:21Z | External review is defense in depth, not an oracle; findings used as evidence need explicit accepted/refuted/deferred dispositions and proof. |
| CTX-9 | repository | The confirmed #56 boundary explicitly makes template changes author-side forcing functions rather than schema enforcement and leaves existing changes untouched; machine enforcement is therefore new intent, not a rewrite of that confirmed history. | `git:8fd903e:openspec/changes/canon-template-gaps-v0/intent.md#L49-L56` | Read the cited non-goals; expect NG-3 to deny schema enforcement and NG-4 to preserve existing artifacts. | 2026-08-02T18:16:34Z | The versioned conformance contract belongs in a dependent change with an explicit legacy policy. |
| CTX-10 | repository | Goalrail's existing pre-PR design makes review advisory and acknowledges that review findings can be ignored; it does not make a reviewer authoritative. | `git:8fd903e:openspec/changes/pre-pr-review-v0/design.md#L189-L193` | Read the cited risk; expect the design to say Goalrail cannot force action on findings and that the review is advisory. | 2026-08-02T18:16:34Z | The new slice may make dispositions verifiable without silently converting an external reviewer into a gate or decision owner. |

## Material Unknowns

None. The exact Markdown spelling, compatibility aliases, diagnostic structure,
corpus layout, and compare-before-write mechanism are design decisions bounded
by the candidate intent; none changes the requested owner-visible outcome.
