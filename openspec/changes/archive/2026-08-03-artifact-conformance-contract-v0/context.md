# Context Pack

- **Context Pack ID:** CTXP-artifact-conformance-contract-v0
- **Version:** 2
- **Previous version:** 1
- **Started at:** 2026-08-03T06:19:46Z
- **Completed at:** 2026-08-03T07:31:10Z
- **Outcome:** sufficient

## Context Items

| ID | Kind | Claim | Source | Verification recipe | Observed at | Relevance |
|---|---|---|---|---|---|---|
| CTX-1 | repository | The current source of truth is `origin/main` at `607e6c11a175cff5bb61c39e437c8a8fd19c6310`, containing the squash merges of PRs #56 and #57; this worktree still points at the superseded pre-merge branch head `8fd903e`. | git:origin/main@607e6c11 | Run `git rev-parse origin/main`, `git rev-parse HEAD`, and `git log --left-right --cherry-pick --oneline HEAD...origin/main`; expect `607e6c11...`, `8fd903e...`, and the two squash commits on the main side. | 2026-08-03T07:31:10Z | Planning must use the merged implementation and must not treat the stale worktree head as current repository fact. |
| CTX-2 | repository | The bound intent reader still accepts only the exact Context Pack declaration `<id> version <number>`, while both active confirmed changes on main declare `<id> v<number>`; the current draft also uses the legacy `v1` spelling. | git:origin/main@607e6c11 | Read `parseContextPackDeclaration`, both active intent declarations, and this draft declaration; expect the parser's single `version` grammar and the three `vN` declarations. | 2026-08-03T07:31:10Z | The owner's format complaint remains real, but the affected examples are now the two active changes and this dependent draft rather than the repaired #56 artifact. |
| CTX-3 | repository | PRs #56 and #57 repaired and archived `canon-template-gaps-v0`: its current intent declares `version 3`, and one regression now loads the real pair through `LoadChange` and verifies it through `IntentResolver`. | git:origin/main@607e6c11 | Read the archived intent and `adapter_canon_test.go`, then run `go test ./internal/adapters/openspec` on `origin/main`; expect the bound compiler and resolver checks to pass. | 2026-08-03T07:31:10Z | The new contract must retain this production-bound regression but must not describe the repaired #56 pair as still broken. |
| CTX-4 | repository | Verification recipes are now retained in `domain.ContextItem`, required for seven-column Context Packs, and deliberately empty for supported six-column legacy packs. | git:origin/main@607e6c11 | Inspect `internal/domain/types.go`, `parseContextItems`, and their tests; expect seven-column preservation and six-column compatibility without invented recipe content. | 2026-08-03T07:31:10Z | Recipe preservation is an existing baseline for conformance, not implementation work for this change. |
| CTX-5 | repository | Harness update now performs bounded optimistic compare-before-write checks and preserves recovery evidence, with an explicit residual boundary: a non-cooperating writer may still change a path after the final portable digest read. | git:origin/main@607e6c11 | Inspect the update retry loop, just-in-time digest check, and normative harness-update spec; run the targeted harness update tests and expect the race controls to pass. | 2026-08-03T07:31:10Z | The v1 outcome demanding that a post-inspection edit can never be overwritten is obsolete and stronger than the accepted portable contract. |
| CTX-6 | repository | Ambient active-intent discovery still reads `intent.md` without its sibling Context Pack and silently continues after read or parse failure; a test explicitly accepts one malformed change being hidden behind a valid one. | git:origin/main:internal/ambient/intent.go@L45-L89 | Read the loop and `intent_test.go` malformed-plus-valid test; expect errors to take an unreported `continue` path and the valid intent to bind. | 2026-08-03T07:31:10Z | A malformed artifact that claims confirmed status remains invisible in a real consumer and can be masked by another change. |
| CTX-7 | repository | `LoadChange` is a development compiler with no non-test caller; it is a distinct conformance lane, not evidence for local-run or ambient behavior. | codegraph:LoadChange@607e6c11 | Trace inbound calls to `LoadChange` with tests excluded; expect none. As a fallback, search Go call sites and expect only tests outside the definition. | 2026-08-03T07:31:10Z | Conformance must name the compiler, local-run resolver, and ambient resolver separately instead of calling all three production paths. |
| CTX-8 | repository | Parser and resolver failures are composed from sentinels plus `fmt.Errorf` strings; they do not carry a shared structured diagnostic with artifact kind, contract mode, field or section, bounded observation, expectation, and repair hint. | git:origin/main:internal/adapters/openspec/adapter.go@L23-L832 | Inspect parser error construction and `work_spec_intent.go` resolver wrapping; expect prose errors and no common diagnostic value exposing the listed fields. | 2026-08-03T07:31:10Z | The user still receives format complaints that identify parser expectations incompletely and cannot be rendered consistently across consumers. |
| CTX-9 | repository | Context Pack and Intent Snapshot have artifact versions, and the OpenSpec schema has its own version, but generated artifacts declare no separate machine contract identifier or contract version. | git:origin/main:openspec/schemas/goalrail-intent/templates | Read the domain types, schema header, and both templates; expect artifact versions and schema version 2, but no artifact-contract declaration. | 2026-08-03T07:31:10Z | Compatibility cannot be selected deliberately until contract identity is distinct from artifact lineage and compiler schema revision. |
| CTX-10 | repository | Compatibility behavior is distributed across hand-written tests, with no repository-owned manifest that pins each Context Pack and Intent pair's provenance, input mode, expected semantics or diagnostic, and applicable consumer lanes. | git:origin/main:internal/adapters/openspec | Search the adapter and ambient test packages for a conformance manifest carrying provenance, mode, expected result, and lane metadata; expect individual fixtures and tests but no such manifest. | 2026-08-03T07:31:10Z | A stable corpus is needed to make contract compatibility reviewable without treating every active or archived change as a permanent implicit specification. |
| CTX-11 | repository | Goalrail v0.1.8 contains the same strict Context Pack declaration grammar, so executing that binary as a supposedly known-good compatibility oracle would reproduce the incompatibility this change is meant to resolve. | git:v0.1.8:internal/adapters/openspec/adapter.go@L356-L366 | Run `git show v0.1.8:internal/adapters/openspec/adapter.go` and inspect `parseContextPackDeclaration`; expect only `<id> version <number>`. | 2026-08-03T07:31:10Z | Compatibility expectations need an explicit reviewed corpus; differential replay against the old binary would duplicate the defect or require exception lists that restate the corpus. |
| CTX-12 | repository | All #56 and #57 findings were dispositioned before merge, while the confirmed pre-PR contract keeps finding disposition at the workspace level and forbids Goalrail from interpreting review text. | git:origin/main@607e6c11 | Read the archived completion tasks plus `pre-pr-review-v0` NG-1, NG-4, and design correlation section; expect completed dispositions, workspace ownership, and no product interpretation. | 2026-08-03T07:31:10Z | A new product-level disposition register would duplicate completed process evidence and conflict with an existing confirmed boundary. |

## Material Unknowns

None.
