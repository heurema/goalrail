# Context Pack

- **Context Pack ID:** context-admission-hardening-after-pr62-v1
- **Version:** 1
- **Previous version:** pending
- **Artifact Contract:** goalrail-context-intent
- **Artifact Contract Version:** 1
- **Started at:** 2026-08-05T16:18:11Z
- **Completed at:** 2026-08-05T16:18:11Z
- **Outcome:** sufficient

## Context Items

| ID | Kind | Claim | Source | Verification recipe | Observed at | Relevance |
|---|---|---|---|---|---|---|
| CTX-1 | external | PR #62 was merged at `2026-08-05T15:42:14Z`; its automated review was submitted at `2026-08-05T16:02:56Z` with nine unresolved, non-outdated inline threads: five P1 and four P2. | `github:heurema/goalrail#62`; review `PRR_kwDOTewKTc8AAAABIg7nZA` | With authenticated read access, query PR #62 metadata and GraphQL `reviewThreads`; expect state `MERGED`, the stated timestamps, and nine threads with `isResolved=false` and `isOutdated=false`. | 2026-08-05T16:18:11Z | Establishes that material feedback arrived after merge and defines the bounded remediation set. |
| CTX-2 | repository | The reviewed head `fedcfc16f5c61951ea9cf29a90a91f5ef8a933ea` and squash-merge commit `6a896355958874a4989eec131c6cc20a24921995` have identical trees. | `git:fedcfc16f5c61951ea9cf29a90a91f5ef8a933ea`; `git:6a896355958874a4989eec131c6cc20a24921995` | In this repository, run `git diff --quiet fedcfc16f5c61951ea9cf29a90a91f5ef8a933ea 6a896355958874a4989eec131c6cc20a24921995`; expect exit 0. | 2026-08-05T16:18:11Z | Makes every reviewed finding directly applicable to current `main`. |
| CTX-3 | repository | Shared admission accepts a branch-controlled `owner_decision` event when its `actor_ref` matches policy and it has one target; repository-root targets bypass provider provenance. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108655`; `git:6a896355/internal/admission/evaluate.go` | Read `ownerDecisionAuthorized`, `verifyPacketEvidence`, and `trustedWithoutPacket`; expect actor-and-cardinality authorization plus unconditional trust for `repo:root/` targets. | 2026-08-05T16:18:11Z | A material authorization bypass contradicts the owner-decision boundary. |
| CTX-4 | repository | The GitHub collector appends authenticated pull-request, review, check, and owner-decision references to the admission packet, while lifecycle completeness is computed from the committed graph alone. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108695`; `git:6a896355/internal/githubadmission/collector.go`; `git:6a896355/internal/admission/evaluate.go` | Read `githubadmission.Collect`, `admission.Verify`, and `EvaluateCompleteness`; expect provider references to mutate only the packet and completeness to receive only `FrozenRange.Graph`. | 2026-08-05T16:18:11Z | The prepared shared workflow cannot reach `VALID` from ordinary live provider evidence. |
| CTX-5 | repository | Completeness counts any non-`provider_unavailable` terminal-receipt target as present without decoding the receipt or requiring `status: passed`. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108709`; `git:6a896355/internal/admission/completeness.go` | Read `EvaluateCompleteness`; expect only artifact-kind exclusion and exception decoding, with no terminal-receipt status validation. | 2026-08-05T16:18:11Z | Failed or incomplete execution evidence can satisfy a required admission relation. |
| CTX-6 | repository | For a non-bootstrap range, authority resolution validates head governance internally but returns base governance without requiring the head declaration to retain the base `project_id`. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108716`; `git:6a896355/internal/admission/evaluate.go` | Read `resolveAuthority`; expect no comparison between base and head project identities before returning base governance. | 2026-08-05T16:18:11Z | An admitted change can replace durable project identity outside an explicit migration path. |
| CTX-7 | repository | Shared admission checks intent artifact kind, digest targeting, and graph binding but does not decode the retained snapshot or require confirmed semantics. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108733`; `git:6a896355/internal/admission/evaluate.go` | Read `verifyGraphBinding`; expect `ArtifactKind == "intent"` and relation targeting checks with no call to `ValidateIntentSnapshot` or equivalent semantic validation. | 2026-08-05T16:18:11Z | Candidate intent can be represented as confirmed lineage, bypassing active owner confirmation. |
| CTX-8 | repository | Setup planning emits a complete `select-executable` replacement for an existing same-size executable with a different digest, but apply recovery preparation rejects that exact replacement. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108662`; `git:6a896355/internal/setup/plan.go`; `git:6a896355/internal/setup/rollback.go` | Read `inspectExecutableDestination` and `recoveryStore.prepare`; expect the planner to add the mutation and prepare to reject `exists && digest != DesiredDigest`. | 2026-08-05T16:18:11Z | An owner can authorize a plan that deterministically cannot apply. |
| CTX-9 | repository | Admission-packet validation permits nil `EvaluationTime` with provenance, while provenance verification dereferences `packet.EvaluationTime` for non-repository evidence. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108670`; `git:6a896355/internal/domain/admission_v1.go`; `git:6a896355/internal/admission/evaluate.go` | Read `ValidateAdmissionPacket` and `verifyPacketEvidence`; expect nil time to be allowed when `TimeAuthorityRef` is empty and an unconditional `packet.EvaluationTime.UTC()` in provenance comparison. | 2026-08-05T16:18:11Z | A valid packet shape can panic the pure verifier instead of producing canonical denial. |
| CTX-10 | repository | Git governance collection validates the declaration and declared policy but not the declaration-bound bootstrap and setup-profile bytes or digests. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108721`; `git:6a896355/internal/admissiongit/git.go`; `git:6a896355/internal/domain/project.go` | Read `ProjectDeclaration` and `readGovernance`; expect `Policy`, `Bootstrap`, and `SetupProfile` references but reads and digest validation only for `Policy`. | 2026-08-05T16:18:11Z | Durable governance can become internally stale while admission reports it valid. |
| CTX-11 | repository | Prefix matcher path `.` matches only `.` or `./...`, while normalized Git paths omit `./`; the canonical default policy uses `.` as its root catch-all. | `https://github.com/heurema/goalrail/pull/62#discussion_r3722108727`; `git:6a896355/internal/admission/evaluate.go`; `git:6a896355/internal/project/canon/policy.json.tmpl` | Read `ruleMatches` and the canonical policy template; expect prefix construction `TrimSuffix(path, "/") + "/"` and a `material-default` rule with path `.`. | 2026-08-05T16:18:11Z | Root-wide classification and evidence requirements are silently skipped. |
| CTX-12 | repository | Goalrail currently tracks only `ci.yml` and `release.yml` under `.github/workflows`; the prepared Goalrail lineage workflow remains a canon template and is not active in this repository. | `git:6a896355/.github/workflows`; `git:6a896355/internal/project/canon/prepared-admission.yml` | Run `git ls-files '.github/workflows/**' 'internal/project/canon/prepared-admission.yml'`; expect no `.github/workflows/goalrail-lineage.yml`. | 2026-08-05T16:18:11Z | Immediate operational exposure is bounded, but release or activation must remain stopped until remediation. |
| CTX-13 | repository | Existing tests for admission, Git collection, GitHub collection, and setup all pass despite the nine findings. | `task:admission-hardening-after-pr62-audit` | Run `go test ./internal/admission ./internal/admissiongit ./internal/githubadmission ./internal/setup`; expect all packages to pass before remediation tests are added. | 2026-08-05T16:18:11Z | Demonstrates that regression tests for the reviewed negative cases are required. |

## Material Unknowns

None.
