# Intent Snapshot

- **Intent ID:** INT-artifact-conformance-contract-v0
- **Version:** 2
- **Previous version:** 1
- **Status:** confirmed
- **Owner:** Vitaly D.
- **Context Pack:** CTXP-artifact-conformance-contract-v0 version 2
- **Run references:** owner session eba4d3c0-28c5-4861-91aa-9f7aa05617d5; local implementation session, 2026-08-02 through 2026-08-03; PRs #56 and #57

## Source Evidence

- **SE-1 — owner:** On 2026-08-02 the owner requested implementation of all recommended ideas, preferred a versioned contract, and called format-only failures unreasonable: "можешь реализовать все предложенные тобой идеи, мне нравится версия контракта очень странно получать ошибки формата".
- **SE-2 — owner:** On 2026-08-03 the owner reviewed the Russian plain-language v2 amendment covering the explicit pair contract, finite legacy mode, actionable diagnostics, compiler/local-run/ambient conformance, stable corpus, and all exclusions; the owner answered "да" and explicitly accepted removing executable old-binary replay and a product-level review-disposition register.
- **SE-3 — repository measurement:** origin/main at 607e6c11 contains the merged #56 and #57 remediation and archive, while this worktree still points at the superseded pre-merge head 8fd903e. See CTX-1 and CTX-3.
- **SE-4 — repository measurement:** The bound reader accepts only the declaration form "<id> version <number>", while the two active confirmed changes and this draft use the legacy vN form; generated artifacts still declare no separate artifact-contract version. See CTX-2 and CTX-9.
- **SE-5 — repository measurement:** Ambient discovery reads intent without sibling context and silently skips malformed input, LoadChange has no non-test caller, and format errors have no shared actionable diagnostic value. See CTX-6 through CTX-8.
- **SE-6 — repository measurement:** Recipe preservation, bounded optimistic update safety, Previous version lineage, real #56 pair verification, and all #56/#57 review dispositions are already implemented and retained on main. See CTX-3 through CTX-5 and CTX-12.
- **SE-7 — repository boundary:** Goalrail v0.1.8 contains the strict declaration behavior, and the confirmed pre-PR contract keeps finding disposition workspace-owned and review advisory. See CTX-11 and CTX-12.

## Desired Outcomes

| ID | Outcome | Verification action | Evidence |
|---|---|---|---|
| OUT-1 | Goalrail selects an explicit repository-owned artifact contract for every Context Pack and Intent Snapshot pair before interpreting its semantics. The contract identifier and version are separate from artifact identity and version and from the OpenSpec schema version. New canon emits one exact contract-v1 form. Artifacts without a contract declaration enter only a bounded legacy mode covering repository-pinned supported forms; a declared contract never falls back to legacy, an unknown version fails closed, and existing artifacts are not rewritten. | Generate a canon pair, prove deterministic contract selection, load every pinned legacy form, and reject an unknown declared version without fallback. | SE-1, SE-2, SE-4, CTX-2, CTX-9 |
| OUT-2 | Every contract or format failure produces a safe actionable diagnostic with a stable code, artifact path and kind, selected contract version or legacy mode, field or section, bounded and redacted observation, accepted expectation, and concrete repair hint. The compiler and local-run paths return that diagnostic. Ambient does not silently discard an active artifact that claims confirmed status: it retains a diagnostic naming the invalid change and leaves the question unbound while preserving the rule that background failure does not block or delay the user session. | Run malformed cases through every applicable consumer, compare the structured diagnostic and rendered message with the case manifest, and confirm ambient retains the invalid change by name without affecting session completion. | SE-1, SE-2, SE-5, CTX-6, CTX-8 |
| OUT-3 | A deterministic repository-owned corpus defines the conformance oracle for Context Pack and Intent Snapshot pairs. It contains contract-v1 positives, pinned legacy positives, and parser, cross-file, and ambient negatives. Every case records provenance, input mode, expected domain semantics or diagnostic, and applicable consumer lanes. Expected results are not regenerated by the implementation under test. | Run one corpus command and confirm every manifest case and declared lane reports its independently pinned result. | SE-2, SE-4, SE-5, CTX-2, CTX-10 |
| OUT-4 | Conformance exercises the pair compiler through LoadChange, the local-run verifier through IntentResolver, and ambient active-intent discovery as separate lanes. A compiler pass is never reported as proof of runtime behavior, and the changing set of active or archived changes never becomes an implicit compatibility specification. | Run a case assigned to all three lanes and confirm three independent results, then add an unrelated active change and confirm the normative corpus remains unchanged. | SE-2, SE-5, CTX-3, CTX-6, CTX-7, CTX-10 |
| OUT-5 | Contract relations pin semantic behavior rather than mere parse success. Supported declaration variants produce equal domain meaning; Context Pack identity or version mutations and unknown declared contracts are rejected; preserved fields round-trip without loss; and the existing six/seven-column, old/new-heading, recipe-preservation, and real-pair regressions remain explicit corpus baselines without reopening already completed fixes. | Run each named relation as an independently addressable case and verify that changing its declaration, binding, diagnostic expectation, or preserved field makes that case fail. | SE-2, SE-4, SE-6, CTX-2, CTX-3, CTX-4, CTX-10 |

## Non-Goals

| ID | Boundary | Evidence |
|---|---|---|
| NG-1 | Contract v1 covers only Context Pack and Intent Snapshot pair semantics and their direct consumers; it does not define a universal Markdown language or version Proposal, Design, Tasks, or every OpenSpec artifact. | SE-2, CTX-7, CTX-9 |
| NG-2 | No parser generator, schema service, database, workflow engine, reviewer provider, or new runtime dependency is introduced. | SE-2, CTX-9 |
| NG-3 | Generic mutation testing and panic-only fuzzing are not part of this slice; neither supplies the missing compatibility oracle or consumer wiring. | SE-2 |
| NG-4 | External model review does not become a correctness oracle, decision owner, product-level disposition register, veto, or mandatory merge gate. Review-derived corpus cases require local provenance, not reviewer authority. | SE-2, SE-7, CTX-12 |
| NG-5 | Current active changes may supply pinned legacy examples, but they are not dynamically scanned normative contract inputs; the whole active or archived tree cannot replace the explicit corpus. | SE-2, CTX-2, CTX-10 |
| NG-6 | Existing active and archived artifacts are not rewritten or migrated. Compatibility is added in this dependent change, and future intentional contract changes require explicit versioned deltas. | SE-2, SE-6, CTX-3, CTX-12 |
| NG-7 | This planning and implementation work grants no permission to commit, push, merge, publish, release, deploy, or modify external systems. | SE-2 |
| NG-8 | Legacy compatibility is a finite allowlist, not permissive parsing, typo correction, alias discovery, or fallback from a malformed declared contract. | SE-2, SE-4, CTX-2 |
| NG-9 | Compatibility is not defined by differential execution against an older Goalrail binary; v0.1.8 carries the strict behavior being replaced. | SE-2, SE-7, CTX-11 |
| NG-10 | This change does not modify harness update concurrency, previous-canon migration, Verification recipe retention, or Previous version lineage; those guarantees already exist on main and remain protected by their current tests and specifications. | SE-2, SE-6, CTX-3, CTX-4, CTX-5, CTX-12 |

## Observable Success Signals

| ID | Signal | Measurement | Evidence |
|---|---|---|---|
| SIG-1 | Canon and the direct consumers agree on contract v1. | A generated pair declares contract v1 and produces equal domain semantics through the pair compiler and IntentResolver. | SE-2, SE-4, CTX-9 |
| SIG-2 | Existing supported artifacts are not stranded. | Pinned copies of the current openspec-adoption-v0 v1 declaration, pre-pr-review-v0 v4 declaration, and representative six/seven-column and old/new-heading artifacts load in legacy mode with expected semantics. | SE-2, SE-4, SE-6, CTX-2, CTX-4 |
| SIG-3 | Invalid declarations fail usefully and never downgrade silently. | Unknown contract versions, cross-file identity or version mutations, and malformed confirmed intents return the exact safe diagnostic; no declared-contract failure is retried as legacy. | SE-2, SE-5, CTX-6, CTX-8 |
| SIG-4 | Ambient no longer hides a broken confirmed intent. | A malformed active artifact claiming confirmed status is named in retained diagnostic evidence, leaves binding unbound, and does not block or delay the session. | SE-2, SE-5, CTX-6 |
| SIG-5 | The oracle is stable and traceable across its declared lanes. | One deterministic command reports 100 percent of manifest cases across applicable compiler, local-run, and ambient lanes; every case has provenance and independently pinned expected semantics or diagnostic. | SE-2, SE-4, SE-5, CTX-7, CTX-10 |
| SIG-7 | The repository remains healthy after the bounded slice. | Targeted adapter, ambient, and local-run tests, race tests for touched packages, full Go tests, strict OpenSpec validation, and a clean diff audit pass before handoff. | SE-2, SE-6, CTX-3, CTX-4, CTX-5 |

## Ambiguities and Unknowns

None.

## Confirmation

- **Confirmed by:** Vitaly D.
- **Confirmed at:** 2026-08-03T07:52:26Z
- **Verification action:** The owner read the Russian plain-language v2 view covering the explicit pair contract, finite legacy mode, actionable diagnostics, three conformance lanes, stable corpus, ambient behavior, all exclusions, and the removal of executable old-binary replay and a product-level review register, then answered "да" to the exact question confirming INT-artifact-conformance-contract-v0 v2.
- **Amendment rule:** A material change to outcomes, non-goals, or success signals creates a new version; wording-only edits preserve this version.
