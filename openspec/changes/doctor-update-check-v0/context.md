# Context Pack

- **Context Pack ID:** context-doctor-update-check-v0
- **Version:** 4
- **Previous version:** 3
- **Started at:** 2026-07-30T18:10:00Z
- **Completed at:** 2026-07-31T07:50:00Z
- **Outcome:** sufficient

Version 2 adds CTX-17, observed after the owner asked how the check is best done
against practice: it settles what the toolchain's own request looks like, which
version 1 had only assumed.

Version 4 appends CTX-19, which corrects CTX-4 — the measurement the owner's
decision rested on. The estimate was half the real cost, and the correction was
put to the owner before any of it was committed.

Version 3 appends CTX-18, which narrows CTX-17 rather than replacing it. It
postdates the intent's confirmation and changes no decision: the inference
CTX-17 supported — that a check setting no agent string is indistinguishable from
toolchain traffic — is exactly what the narrower fact supports.

## Context Items

| ID | Kind | Claim | Source | Observed at | Relevance |
|---|---|---|---|---|---|
| CTX-1 | repository | No promoted requirement forbids network access in `gr` as a whole. The only one that mentions the network is scoped to the update command: "The command MUST NOT attempt a network lookup for a newer release … whether `gr` should consult it remains an open decision with its own intent." The word "network" appears in no other promoted spec. | repo:openspec/specs/harness-update/spec.md:84-89, 95-97; grep across `openspec/specs/**` | 2026-07-30T18:14:00Z | Teaching the diagnosis to check violates no promoted contract, provided `gr update` itself stays inert. This change satisfies a recorded revisit condition rather than amending a prohibition. |
| CTX-2 | repository | The "no network anywhere in `gr`" property lives at intent level, not as a promoted SHALL: it is confirmed non-goal NG-1 of `harness-init-v0` ("No network release check and no self-update … Revisit when a release channel exists") and NG-1 of `release-channel-v0` ("asking it a question is the separate change the revisit condition now unblocks"). | repo:openspec/changes/archive/2026-07-30-harness-init-v0/intent.md:56, .../2026-07-30-release-channel-v0/intent.md:57 | 2026-07-30T18:15:00Z | The property is retired by a new confirmed intent, not by a spec amendment. It also means the owner is being asked to end something two prior changes deliberately kept. |
| CTX-3 | repository | The property is currently structural, not behavioural: `net/http` is absent from `go list -deps ./cmd/gr` entirely — the shipped binary cannot open a connection. The only network-named import it carries is `net/url`, used to strip userinfo from a printed endpoint. The tree's one HTTP client, the Langfuse adapter, is reachable only from the separate canary binary. | local:`go list -deps ./cmd/gr`, `grep -rn '"net/http"'`; repo:internal/harness/observability.go:92 | 2026-07-30T18:16:00Z | Gives the guarantee a testable definition — a test can assert `net/http` is absent from the binary's dependency graph — and shows that adding a check is a structural change, not a behavioural one. |
| CTX-4 | repository | Adding `net/http` to `gr` costs 2,998,400 bytes: the binary grows from 4,969,826 to 6,968,226, +40.2%, measured by adding one import to a scratch copy of `cmd/gr` and rebuilding with `CGO_ENABLED=0`. A `crypto/tls`-only path costs +1,004,800 (+20.2%). | local:scratch copy of the worktree, `go build` before and after adding one import, `stat -f%z` | 2026-07-30T18:20:00Z | Every user of every platform downloads 40% more binary so the tool can ask whether it is current. It is the concrete price of the decision and it is not recoverable by writing the code well. |
| CTX-5 | repository | The diagnosis forbids two shapes this feature could take. Every next action "SHALL be a command this tool accepts — or, where the tool deliberately has no command for the act … SHALL say that plainly rather than inventing a command"; and "An optional facility that is simply absent SHALL be stated without a next action and without contributing a failure." | repo:openspec/specs/harness-doctor/spec.md:26-29, 39-44 | 2026-07-30T18:22:00Z | `gr` has no self-update command, so "a newer release exists" cannot become a problem whose next action names one. The available shapes are an informational line like the toolchain and observability lines, or a next action that plainly states the manual act. |
| CTX-6 | repository | The diagnosis reports facts and problems separately: optional absences take the fact path — Node absent prints its note and adds no problem, observability absent prints "not configured (optional)" with no next action — while problems are index-paired with next actions. Exit codes are 0 healthy, 1 ran and found problems, 2 the check itself did not run. | repo:internal/harness/doctor.go:49-76, 251-305; cmd/gr/harness.go; local:`gr doctor` in scratch repositories | 2026-07-30T18:24:00Z | Fixes the insertion point. An informational line leaves the exit contract untouched; a problem would flip healthy repositories to exit 1 and change what every unattended check means. |
| CTX-7 | repository | The reported version is frequently not a plain release tag: `resolveVersion` returns build information verbatim, and the promoted shapes are the tag at a tag, a pseudo-version between tags, a `+dirty` marker on a modified tree, the toolchain's development marker with no version control, and `unknown` where the build carries nothing. Observed here: `(devel)` under `go run`, `v0.1.2-0.20260730182101-c49814687b66+dirty` from this checkout. | repo:internal/harness/version.go:19-41, openspec/specs/release-channel/spec.md:85-99; local:`go run ./cmd/gr version` | 2026-07-30T18:26:00Z | A comparison against a release tag is undefined for most of these shapes. Whatever ships must say nothing rather than claim an update when the local version is not a released one. |
| CTX-8 | external | `https://proxy.golang.org/<module>/@latest` answers in one small JSON object with the newest **stable** version, skipping prereleases — verified against a module whose tag list contains alphas and betas. It needs no authentication, no User-Agent, and exposed no rate limit across 40 rapid sequential requests; the response carries `cache-control: max-age=60`. Warm latency from this machine was ~0.27s. | external:`curl` against `proxy.golang.org` `@latest`, `@v/list`, `.info` for this module and a comparison module; header and timing captures | 2026-07-30T18:30:00Z | The smallest possible shape for a first network call: one request, one field, no credential, no quota, and no client-side version sorting or prerelease filtering. |
| CTX-9 | external | The proxy serves module source only and knows nothing about release assets: the module zip contains the source tree and no binaries, and asset-shaped endpoints are rejected. `gr`'s deliverables exist only as GitHub release assets. | external:`curl` of the module zip and an invalid endpoint; `gh api` release asset list | 2026-07-30T18:31:00Z | Bounds the feature honestly: the proxy can say a newer version exists and can never hand over the binary. Anything that wants the artifact is a second source with its own limits — a reason to keep this slice to the question, not the download. |
| CTX-10 | external | The proxy learns about a tag late when nobody asks: measured twice for this repository, 3m45s for `v0.1.0` and 14m46s for `v0.1.1` between tag time and first cache, corroborated by two independent sources. Its documentation sets no bound for that passive path, saying only that new versions "may not show up right away", and offers one remedy — explicitly requesting the version, after which it "should be available within one minute". | external:`index.golang.org` ingest timestamps, `last-modified` on `.mod`; proxy.golang.org FAQ | 2026-07-30T18:34:00Z | For roughly a quarter of an hour after a release the check would tell users they are current when they are not. It rules out any wording that claims authority, and it gives a zero-code mitigation: the release workflow can request the new version and collapse the window to about a minute. |
| CTX-11 | external | `GOPROXY`, `GOPRIVATE`, `GONOPROXY` and `GONOSUMDB` are read by the `go` command alone and have no effect on a program making its own HTTPS request: with `GOPROXY=off` the `go` command refuses a module lookup while a direct `curl` to the proxy returns 200. | local:`GOPROXY=off go list -m -versions …` versus `GOPROXY=off … curl …` in one environment | 2026-07-30T18:36:00Z | A user who set `GOPROXY=off` has already said they do not want their module lookups leaving the machine. A hand-rolled request would silently override that. Reading those variables turns an existing, ignored opt-out into a respected one. |
| CTX-12 | external | The GitHub REST alternative costs a shared quota: 60 requests per hour per IP unauthenticated, already partly spent by other tools on the same address, not coherent across GitHub's edges, and a conditional request answered `304` still consumes a unit. It requires a User-Agent — an empty one is refused — and its `404` cannot distinguish a repository with no releases from one that does not exist. | external:`curl` with header capture against `api.github.com`, including conditional requests and `/rate_limit` reads before and after | 2026-07-30T18:38:00Z | On a shared office or CI address the check would fail for reasons invisible to the user, and caching does not buy quota back. It also forces `gr` to self-identify in a header. |
| CTX-13 | external | The cheapest GitHub path is the plain redirect: `https://github.com/<owner>/<repo>/releases/latest` answers `302` with the tag in its `Location`, a zero-byte body, no rate-limit headers, no User-Agent requirement, and ~0.55s. But it is served from GitHub's tracked web surface: it sets three cookies, including `_octo` and `logged_in` with a one-year expiry. The REST answer sets none. | external:`curl -D-` against both endpoints; cookie names read with values discarded | 2026-07-30T18:40:00Z | The faster, quota-free GitHub path is the one that touches analytics; the cleaner one costs quota and a self-identifying header. Neither is free, which is what makes the proxy interesting. |
| CTX-14 | external | Settled practice across every CLI that checks at all — `gh`, Homebrew, Deno, npm's update-notifier: a cache interval of about 24 hours, one environment variable to disable it named for the tool, the notice on standard error, a failed check silent to an ordinary user, the check never blocking the command, and no check in continuous integration or when the output is not a terminal. | external:primary sources — `cli/cli` update.go and its build-tagged enable file, Homebrew `auto-update.sh` and `env_config.rb`, Deno `upgrade.rs`, update-notifier readme | 2026-07-30T18:41:00Z | These six need no argument and no invention. They can be adopted as they stand, which keeps the decision surface down to the genuinely contested choices. |
| CTX-15 | external | What is contested, and where nobody wrote down a rationale: `gh` compiles its notifier **out** of the binaries it ships and lets the packager turn it on; `rustup` has no ambient check at all and instead offers an explicit `rustup check` whose exit status is documented as 0 up to date, 100 update available, 1 error; `kubectl` never contacts an upstream feed, comparing only against the cluster the user already talks to; `hyperfine` carries no HTTP client at all. None of these projects documents a privacy or telemetry justification for the version check itself. | external:`cli/cli` `update_enabled.go`/`update_disabled.go` and its release configuration, `rustup check --help` and its source, `kubectl` skew-warning source, `hyperfine` dependency list | 2026-07-30T18:42:00Z | Two respected tools answer "am I current" with no phone-home at all, so keeping the network out is a citable position rather than a fringe one. `rustup check` is the closest precedent to an explicit `gr doctor`, including a distinct exit status for "update available". And since nobody argued the privacy case in public, this project cannot cite prior art either way and has to make the argument itself. |
| CTX-16 | external | Failure shapes the implementation must survive: the proxy answers `404` for an unknown module with a body that leaks its own internal paths and git errors; a module with no tags answers `200` with a pseudo-version, and its version list answers `200` with an empty body. | external:`curl` against nonexistent modules, an unknown version, and a tagless module | 2026-07-30T18:43:00Z | Status codes alone are not enough, a pseudo-version must never be compared against a release, and the proxy's error bodies must never reach a user as Goalrail output. |

| CTX-17 | repository | The `go` command's own proxy request carries `User-Agent: Go-http-client/1.1` — the standard library's default, meaning it sets none of its own — and no other identifying header. Observed by pointing `GOPROXY` at a local capture server and running `go list -m <module>@latest`. | local:HTTP capture server, `GOPROXY=http://127.0.0.1:… go list -m github.com/heurema/goalrail@latest` | 2026-07-30T19:02:00Z | A check built on the standard library with no custom User-Agent is indistinguishable from ordinary toolchain traffic at the proxy: the only signal in the request is the module path — one a `go install` user's machine already sends routinely. A self-identifying agent string would be the single bit announcing "a gr installation is here", which is the phone-home signal in its purest form. |

| CTX-18 | repository | CTX-17's observation was made against a local cleartext server, so the literal string it recorded is the HTTP/1.1 default. The `go` command sets no agent string of its own; the standard library supplies its default for whichever protocol is negotiated, and the proxy the request actually reaches is served over HTTP/2. The fact that matters is the absence of anything set, not the literal recorded. | local:the capture in CTX-17 was plaintext HTTP/1.1 by construction; protocol negotiated against `proxy.golang.org` is HTTP/2 | 2026-07-30T19:38:00Z | Narrows CTX-17 before it could become a hard-coded literal in an implementation. The requirement is that Goalrail sets nothing, so the request is whatever the standard library would have sent anyway; pinning either string would invert the point. |

| CTX-19 | repository | CTX-4's estimate was wrong by a factor of two, and wrong in a way worth naming: it measured *importing* a transport rather than *using* one. A declared but uncalled client is mostly eliminated by the linker, which is why it read +40.2%. The shipped check calls it, so the stack is retained whole — 78 further packages, 122 to 200 — and the binary grows from 4,969,826 to 9,869,954 bytes, +98.6%. What a user downloads grows with it: the compressed archive goes from 2,760,261 to 5,419,593 bytes, +96%. Plain HTTP measures identically, so the weight is the live HTTP stack rather than TLS alone. | local:build of `main` at `c498146` against the implemented branch, `go list -deps` on both, `tar -czf` on each binary | 2026-07-31T07:48:00Z | The price the owner accepted was half the real one. It was measured as task 10.4 requires, put to them with the corrected figure before anything was committed, and re-decided rather than absorbed. It also fixes the lesson for any future estimate here: measure a used dependency, never an imported one. |

## Material Unknowns

None.

The decisions this evidence frames — whether `gr` gains a network call at all
given its measured price, which source answers the question, whether the check is
explicit or default within the diagnosis, and how the answer is reported without
prescribing a command that does not exist — are owner decisions rather than
evidence gaps. No further collection can settle them, so they are carried into
the candidate Intent Snapshot as recorded ambiguities with a proposed answer each.
