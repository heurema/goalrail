# Context Pack

- **Context Pack ID:** context-harness-init-v0
- **Version:** 2
- **Previous version:** 1
- **Started at:** 2026-07-30T12:00:00Z
- **Completed at:** 2026-07-30T13:30:00Z
- **Outcome:** sufficient

## Context Items

| ID | Kind | Claim | Source | Observed at | Relevance |
|---|---|---|---|---|---|
| CTX-1 | repository | `gr init` writes one file — `.goalrail/ambient.json`, a two-field marker — and reports the path. It installs nothing else: no OpenSpec overlay, no hook registration, no observability wiring. | repo:internal/ambient/ambient.go, cmd/gr/ambient.go | 2026-07-30T12:02:00Z | The gap between "marks a repository" and "installs the harness" is exactly this change's subject. |
| CTX-2 | repository | The harness a repository actually needs is eight files: `openspec/config.yaml` naming the schema, `openspec/schemas/goalrail-intent/schema.yaml`, and six templates (`context`, `intent`, `proposal`, `spec`, `design`, `tasks`). Nothing in the binary knows about them today. | repo:openspec/config.yaml, openspec/schemas/goalrail-intent/** | 2026-07-30T12:04:00Z | Fixes the concrete payload `gr init` must materialize and later compare for drift. |
| CTX-3 | repository | `.gitignore` ignores `.goalrail/`, so the initialization marker is never committed: initialization is per clone, per user, not a shared repository fact. | repo:.gitignore, `git check-ignore .goalrail/ambient.json` | 2026-07-30T12:05:00Z | Whatever `gr init` registers has to hold together for a user who clones an already-harnessed repository, and a per-user marker is the existing precedent for per-user registration. |
| CTX-4 | repository | `gr` has no version of its own: no version command, no version constant, no Git tag, and no `.github` workflow or release pipeline in the repository. | repo:cmd/gr/**, `git tag`, repository root listing | 2026-07-30T12:07:00Z | An update surface expressed as "the only version the user sees is Goalrail's own" has no version to read and no channel to check today; one must be created, and a network release check has nothing to query. |
| CTX-5 | repository | The pinned CLI is invoked as `OPENSPEC_TELEMETRY=0 npx --yes @fission-ai/openspec@1.6.0`, and `AGENTS.md` records that 1.6.0 resolves `new change` to `spec-driven` even though `openspec/config.yaml` names the custom schema, so every change must pass `--schema goalrail-intent` explicitly. | repo:AGENTS.md, openspec/config.yaml | 2026-07-30T12:09:00Z | The pin and this defect workaround are part of what "the harness" means; if `gr` owns the invocation, it owns the workaround too, and a repository that only has the files still fails on the first `new change`. |
| CTX-6 | external | The pinned CLI carries its own `init`, `update`, and `doctor` commands. `init --tools <list|all|none>` configures agent tool files non-interactively; `update` refreshes those instruction files; `doctor` reports relationship health for the resolved root. `schema` offers `which`, `validate`, `fork`, and `init`. | local:openspec-1.6.0 `--help`, `init --help`, `doctor --help`, `update --help`, `schema --help` | 2026-07-30T12:12:00Z | Three of Goalrail's intended command names already exist inside the dependency with different meanings, and `schema validate` plus `doctor` are reusable checks rather than things to reimplement. |
| CTX-7 | repository | `.codex/skills/openspec-*` are six provider skill packs whose frontmatter records `generatedBy: 1.6.0`, and their instructions call a bare `openspec` executable with no pin, no telemetry flag, and no `--schema` argument. | repo:.codex/skills/openspec-propose/SKILL.md and siblings | 2026-07-30T12:14:00Z | Evidence that the dependency's own `init` writes provider prompt packs into a repository, and that the packs it wrote here disagree with this project's recorded invocation contract. Materializing them is therefore a decision, not a default. |
| CTX-8 | repository | Baseline verification is green before this change: strict validation of the pinned CLI reports 10 passed, 0 failed across every promoted spec. | local:`openspec validate --all --strict` at af1e4be | 2026-07-30T12:16:00Z | Establishes the pre-change baseline that this change's own artifacts must not break. |
| CTX-9 | repository | The promoted `ambient-connect` requirement states that attachment is registered at user scope, names that as a recorded limitation rather than a preference, attributes it to the first scaffold — where project-local hooks are silently skipped and the session-opening event has passed before trust could be granted — and names the second scaffold's project-level settings layer as the first thing to revisit. | repo:openspec/specs/ambient-connect/spec.md | 2026-07-30T12:18:00Z | The promoted text this change modifies, and the reason the modification is scaffold-specific rather than global. |
| CTX-10 | repository | One live non-interactive Claude Code run established that the announcement reaches the agent and that a question left at the reserved path is retained outside the repository with its own identity and an explicit unbound reason. The same record states what it did not establish: whether the scaffold gates a registered hook behind user approval, because the run was non-interactive and the hooks were supplied per run rather than registered. | repo:openspec/changes/archive/2026-07-30-observe-claude-code-live-v0/** | 2026-07-30T12:20:00Z | The mechanism is observed; only the approval question is open, and it is the question the owner asked to answer inside this work rather than with a throwaway registration. |
| CTX-11 | external | The provider documents `/hooks` as a read-only browser of configured hooks, states that changing hooks means editing the settings JSON directly, and records no approval or review gate before a hook configured in a settings file runs. The one trust-gated case it names is frontmatter hooks in a project subagent. | external:code.claude.com/docs/en/hooks | 2026-07-30T12:24:00Z | Documented evidence that project-scope registration on this scaffold likely needs no trust step at all — which is what a disclosure must not invent — while remaining documentation rather than observation. |
| CTX-12 | external | The provider's workspace trust dialog gates `permissions.allow` rules and `permissions.additionalDirectories` from a project's `.claude/settings.json`; `deny` and `ask` are unaffected because they only restrict. Trust is saved per workspace keyed on the git repository root, and in non-interactive `-p` mode no dialog appears and the gated rules stay ignored. Hooks are not named among what the dialog gates. | external:code.claude.com/docs/en/permissions | 2026-07-30T12:26:00Z | Names precisely what the second scaffold's only documented consent gate covers, so the disclosure can state the truth instead of pointing at a screen that gates something else. |
| CTX-13 | external | The provider's settings precedence is `~/.claude/settings.json` for all projects, `.claude/settings.json` for one project and shareable through the repository, and `.claude/settings.local.json` for one project and not shared — described as the user's own file and normally git-ignored. | external:code.claude.com/docs/en/hooks, code.claude.com/docs/en/permissions | 2026-07-30T12:28:00Z | Project scope is two different arrangements, not one: a committed file that installs hooks for every teammate who opens the repository, and a per-user file that installs them for the person who ran the command. |
| CTX-14 | repository | The existing Langfuse adapter is a bounded read-only client of the observations API plus a reference translator; it holds credentials in adapter memory, performs no writes, and owns no execution lineage. No exporter, span emitter, or trace writer exists anywhere in the tree. | repo:internal/adapters/langfuse/client.go, references.go | 2026-07-30T12:32:00Z | "The existing Langfuse adapter is the first implementation of the exporter interface" does not hold as stated: an exporter would be new code with no current caller, which the project's own scope discipline forbids until a measured need exists. |
| CTX-15 | repository | The state root resolves from `GOALRAIL_STATE_HOME`, then `XDG_STATE_HOME/goalrail`, then `~/.local/state/goalrail`, is created with `0o700`, and refuses the filesystem root. It is the existing home for everything that must live outside the repository. | repo:internal/localrun/store.go | 2026-07-30T12:34:00Z | Gives observability configuration and any update record a place that is neither the repository nor the owner's scaffold configuration, so no credential ever needs to enter a committed file. |
| CTX-16 | repository | `gr health` reports one attachment per scaffold with `connected`, `initialized`, `trust`, `working`, a next action, an `unverifiable` list, and a distinct `config_error` state; with no `--scaffold` it reports every supported scaffold. Its promoted requirement forbids writing, computing, or reproducing a trust record, and requires the report to name what it could not verify. | repo:internal/ambient/health.go, openspec/specs/ambient-connect/spec.md | 2026-07-30T12:36:00Z | `gr doctor` grows from this rather than replacing it: the state machine, the honesty rules, and the per-scaffold fan-out already exist and are promoted contract. |
| CTX-17 | repository | Connection and repair notices in code send the user to `gr health` by name, and the promoted help requirement requires the health command to be discoverable from `gr help` because "registered but not trusted" is otherwise indistinguishable from a broken install. | repo:internal/ambient/health.go, openspec/specs/operator-cli-help/spec.md | 2026-07-30T12:38:00Z | Renaming the surface to `doctor` breaks printed remedies and promoted help text unless the old name keeps working and both are updated together. |
| CTX-18 | repository | The owner's boundaries for this session: their working `~/.claude/settings.json` and `~/.codex/config.toml` are not to be touched, branch `codex/dogfood-run-v0` holds deferred work, commit, push, pull request, merge, archival, and any provider run each need a separate explicit instruction, and the gate is recorded in `tasks.md` before the action rather than after. | owner statement, 2026-07-30 session | 2026-07-30T12:40:00Z | Bounds what this change may verify locally and fixes the recording order for every gate in `tasks.md`. |
| CTX-19 | external | The pinned CLI's package is distributed only through npm and requires Node.js 20.19 or newer; the project publishes no standalone executable. | external:npmjs.com/package/@fission-ai/openspec, github.com/Fission-AI/OpenSpec (installation.md, releases) | 2026-07-30T13:10:00Z | Any `gr` behaviour that shells out to the pinned CLI inherits a Node runtime and, on first use, a network fetch. The owner directed that `gr`'s own commands must not depend on Node, so validation of the canon has to happen before release rather than in the user's repository, and removing Node from the agent-facing workflow entirely would mean replacing the stock CLI — a separate decision this change does not make. |

## Material Unknowns

| ID | Question | Sources |
|---|---|---|
| CTXQ-1 | Whether the second scaffold, in an interactive session, runs a hook registered in a repository-scoped settings file without any approval step. Documentation records no such gate and names the workspace trust dialog as covering permission rules rather than hooks (CTX-11, CTX-12), which is enough to choose the arrangement but is not an observation. | external:code.claude.com/docs/en/hooks, code.claude.com/docs/en/permissions; repo:openspec/changes/archive/2026-07-30-observe-claude-code-live-v0/** |

`CTXQ-1` does not block intent confirmation, and the reason is worth stating
rather than assuming. Both answers lead to the same registration arrangement:
the existing per-scaffold disclosure discipline already distinguishes an
asserted trust step from an unverified one, so the answer changes the wording of
one notice and the content of one doctor line, not what `gr init` installs or
where. It stays recorded as a material unknown because this change is where the
owner asked it to be answered — by one interactive observation of the shipping
arrangement, which is a provider run behind its own owner gate.
