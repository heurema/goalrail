# Intent Snapshot

- **Intent ID:** intent-harness-init-v0
- **Version:** 2
- **Status:** confirmed
- **Owner:** repository owner
- **Context Pack:** context-harness-init-v0 version 2
- **Run references:** none yet; one interactive observation of the shipping arrangement is planned and remains behind its own owner gate
- **Previous version:** 1 (`intent-v1.md`), amended after an owner-directed critical review: `gr` becomes Node-free, update stops on drift by default, behind-ness is judged by digests rather than a recorded version, initialization detects the scaffold, and the registration's git-ignore precondition, legacy-migration advice, and repair-on-rerun are made explicit.

## Source Evidence

- **SE-1 (owner):** `gr init` grows from marking a repository into installing the whole harness — the OpenSpec overlay with the Goalrail schema, the session-hook registration, and optional observability wiring.
- **SE-2 (owner):** `gr doctor` grows out of `gr health` into full diagnosis, including drift between a repository's copy of the schema and the canonical one, and including whether an update is available.
- **SE-3 (owner):** Updating becomes one command. The only version a user sees is Goalrail's own; OpenSpec compatibility is verified internally and its pin ships inside a Goalrail release; the user is never asked to reason about OpenSpec versions. Updating is never silent — seamless means one command, not zero.
- **SE-4 (owner):** The customization is an overlay, not a fork. The CLI stays stock; the canonical schema lives in the `gr` binary and is materialized into each repository; drift is detected by comparing digests rather than by trusting the copy.
- **SE-5 (owner):** Goalrail works fully with no observability backend — the local state root is the source of truth, and exporters are removable attachments. v0 wires only an instance the user already has; absence produces one non-nagging line. Hosted multi-tenant observability is deferred because traces carry prompts and user code.
- **SE-6 (owner):** Registration scope belongs to this work. Project scope is structurally better: registered per repository, the hook is never invoked in unrelated sessions at all, so the boundary is a property of where it is installed rather than of Goalrail's first act. `gr init` is its natural home, and for the second scaffold a separate connection step then has no reason to exist. The open approval question is answered here, in the arrangement that will ship, not by a throwaway registration in the owner's working configuration.
- **SE-7 (owner, this session):** This round produces planning artifacts only. The owner's working `~/.claude/settings.json` and `~/.codex/config.toml` are not to be touched; branch `codex/dogfood-run-v0` holds deferred work; commit, push, pull request, merge, archival, and any provider run each require a separate explicit instruction, recorded in `tasks.md` before the action.
- **SE-8 (repository):** `gr init` writes one marker file and nothing else, and the marker is git-ignored, so initialization is already a per-clone, per-user act (CTX-1, CTX-3).
- **SE-9 (repository):** `gr` has no version, no tag, and no release pipeline, so "an update is available" has neither a version to read nor a channel to query today (CTX-4).
- **SE-10 (repository):** The pinned invocation carries a recorded defect workaround — 1.6.0 resolves `new change` to the default schema unless `--schema goalrail-intent` is passed explicitly — so a repository holding only the overlay files still fails on its first change (CTX-5).
- **SE-11 (repository):** The dependency already owns the names `init`, `update`, and `doctor` with different meanings, and offers `schema validate` and `doctor` as reusable checks (CTX-6).
- **SE-12 (repository):** The dependency's own initialization writes provider prompt packs; the six it wrote into this repository call an unpinned executable without the schema argument, so they disagree with this project's invocation contract (CTX-7).
- **SE-13 (external):** The second scaffold documents no approval step for a hook configured in a settings file, documents `/hooks` as read-only, and gates its workspace trust dialog on permission rules and additional directories rather than on hooks (CTX-11, CTX-12).
- **SE-14 (external):** That scaffold's project scope is two different arrangements: a committed settings file shared with every teammate who opens the repository, and a per-user project file that is not shared (CTX-13).
- **SE-15 (repository):** The existing Langfuse adapter is a bounded read-only reader with no exporter, span emitter, or trace writer anywhere in the tree, so "the existing adapter is the exporter interface's first implementation" does not hold as stated (CTX-14).
- **SE-16 (repository):** `gr health` already reports per-scaffold attachment state with next actions, an explicit unverifiable list, and a distinct unreadable-configuration state, under a promoted requirement that forbids forging trust and requires naming what was not verified (CTX-16).
- **SE-17 (repository):** Printed remedies and promoted help text name `gr health` by name, so renaming the surface breaks advice the tool gives unless both move together and the old name keeps working (CTX-17).
- **SE-18 (external):** The pinned CLI is distributed only through npm and requires Node.js 20.19 or newer; no standalone executable exists. Any `gr` behaviour that shells out to it inherits a Node runtime and a first-use network fetch (CTX-19).
- **SE-19 (owner, this session):** `gr`'s own commands must not depend on Node. The canon is verified with the pinned CLI on Goalrail's side before it is embedded; in the user's repository, digests decide. Node remains only where the user's agents run the stock CLI itself, and removing it from there would mean replacing OpenSpec — a separate decision, recorded as a revisit condition rather than made here.
- **SE-20 (owner, this session):** Three review decisions: `gr update` stops on drift by default and overwrites only on an explicit flag; whether a repository's harness is behind the binary is judged by comparing overlay digests, not by a version recorded in the repository; `gr init` selects the scaffold by detection with a flag override.

## Desired Outcomes

| ID | Confirmed wording | Verification action | Evidence |
|---|---|---|---|
| OUT-1 | One command installs the harness in one repository: `gr init` materializes the canonical OpenSpec overlay — the config naming the Goalrail schema, the schema, and its templates — from copies embedded in the `gr` binary, alongside the existing marker, and reports what it created, what it changed, what it left alone, the exact pinned invocation the repository is now driven by, and that the new files are the user's to commit. It invokes no Node runtime and performs no network access. Repeating it changes nothing. | Run `gr init` in an empty directory with no Node on the path and read the report; validate the materialized overlay with the pinned CLI on the development side; run `gr init` again and diff the repository. | SE-1, SE-4, SE-8, SE-10, SE-19, CTX-2, CTX-5, CTX-19 |
| OUT-2 | Initialization on a repository that already has OpenSpec adds the Goalrail schema and switches the config to it, touches no existing spec or change file, and reports what changed. When the config already names a custom schema that is not Goalrail's, initialization stops and asks rather than silently rewiring someone else's workflow. | Seed a repository with a stock OpenSpec root and one existing spec and change, run `gr init`, and confirm both survive byte-identical while the config names the Goalrail schema; seed a config naming a foreign custom schema and confirm initialization refuses without an explicit confirmation. | SE-1, SE-4, CTX-2 |
| OUT-3 | The session-hook registration happens inside `gr init`, per repository, for the scaffold whose settings layer allows it. The scaffold is selected by detection, with a flag override; where no supported scaffold is detected, the overlay is still installed and the report says which command registers the attachment later. The registration is written to that scaffold's per-user project settings file rather than a shared committed one, and only after verifying that Git ignores that file — a registration that could be committed would install hooks for teammates, whose consent is not the user's to give. Re-running `gr init` repairs a registration that is stale or unscoped under the same replace-not-accompany discipline as connection, and reports the first scaffold's user-scope connection command as the remaining separate step where that scaffold requires it. `gr connect` for the repository-scope scaffold writes nothing and points at `gr init`. | Run `gr init` in a scratch repository with a fake home: confirm the registration is in the per-user project settings file, the user-level configuration is byte-identical, an unignored settings path blocks registration with a loud report, a stale registration is repaired in place on re-run, and `gr connect` for that scaffold refuses and names `gr init`. | SE-6, SE-14, SE-20, CTX-9, CTX-13 |
| OUT-4 | Removal stays complete and narrow: disconnection removes a repository-scope registration with no residue, leaves any entry it did not add untouched, and is safe to repeat. A legacy user-scope registration for the repository-scope scaffold is never modified by `gr init`; the diagnosis names it and prescribes the consented disconnection command, so the migration passes through the user's explicit act. | Initialize, disconnect, and confirm no managed handler and no empty residue remain while a seeded foreign handler in the same file survives unchanged; seed a legacy user-scope registration and confirm `gr init` leaves it byte-identical while `gr doctor` names it and the command that removes it. | SE-6, CTX-9 |
| OUT-5 | The approval question is answered inside this work, for the arrangement that ships: the disclosure states what is actually established for that scaffold — documented as having no per-hook approval step, with its folder-level trust dialog covering permission rules rather than hooks — and one interactive session with the registered arrangement upgrades that from documented to observed, or records what is observed instead. Goalrail still never writes, computes, reproduces, or simulates a trust record. | Read the disclosure text against the documented evidence, then run one interactive session in a scratch repository initialized by `gr init` and record what the registration did and did not require. | SE-6, SE-13, CTX-10, CTX-11, CTX-12 |
| OUT-6 | `gr doctor` reports one diagnosis of the harness in this repository: per-scaffold attachment state as `gr health` reports it today — including a repository-scope registration and a legacy user-scope one — overlay presence and per-file digest drift against the binary's canonical copy, whether the repository's overlay is behind the binary's canon (judged by digests alone), an environment line stating whether the Node runtime the stock CLI needs is present, and one line for observability. Every state that is not working names its next action; an optional facility that is simply absent — observability, or Node itself — produces a stated fact and no failure. The diagnosis is readable by machines as well: structured output and an exit code that distinguishes healthy from not. | Run `gr doctor` against a healthy repository, a repository with an edited template, one with no overlay, one whose overlay is behind the binary's canon, one with a legacy user-scope registration, one without Node on the path, and one with no observability, and read each report and exit code. | SE-2, SE-4, SE-5, SE-16, SE-19, SE-20, CTX-2, CTX-16, CTX-19 |
| OUT-7 | A remedy the tool prescribes is a command the tool has: `gr health` keeps working as an alias that names its successor, and every printed remedy, help entry, and next action moves to `gr doctor` in the same change. | Run `gr health`, confirm it works and names `gr doctor`, and confirm no printed remedy names a command that does not exist. | SE-2, SE-17, CTX-17 |
| OUT-8 | `gr update` brings a repository's harness up to what the installed `gr` carries, in one command and without Node: it re-materializes the overlay from the embedded canon, verifies the result by digest, reports every file it rewrote and what the repository moved from and to, and leaves the previous state recoverable. When any overlay file has drifted from the canon it was materialized from, update stops and names the drifted files instead of overwriting them; only an explicit flag discards local edits. It never runs as a side effect of another command, and its help states plainly that it does not update the `gr` binary itself. | Initialize from an older embedded canon, run `gr update`, read the report, and restore the previous state from what the command left behind; edit one template and confirm update refuses, names the file, and proceeds only with the explicit flag. | SE-3, SE-4, SE-19, SE-20, CTX-2 |
| OUT-9 | `gr` carries a version of its own that the tool reports, and OpenSpec's version stays internal: no command asks the user to reason about it, and the pin moves only inside a Goalrail change. The version is information about the binary; no repository fact depends on reading it, because overlay currency is decided by digests. | Read the version from the tool, confirm no user-facing surface asks about an OpenSpec version, and confirm doctor's behind-ness verdict is unchanged when the version string alone is altered. | SE-3, SE-9, SE-20, CTX-4 |
| OUT-10 | Observability stays optional in the strong sense: no backend is a fully working configuration; an endpoint and keys already present in the environment or the state root are reported as configured; no credential is ever written into the repository, and no report ever echoes a key — the observability line names the endpoint at most. | Run `gr doctor` with and without observability configured, confirm the repository holds no credential in either case, and confirm no output contains key material. | SE-5, CTX-15 |

## Non-Goals

| ID | Confirmed boundary | Evidence |
|---|---|---|
| NG-1 | No network release check and no self-update of the `gr` binary. There is no tag, no release pipeline, and no channel to query, so `gr update` updates a repository's harness to what the installed `gr` already carries. Revisit when a release channel exists. | SE-9, CTX-4 |
| NG-2 | No exporter interface, span emitter, or trace writer, and no change to the Langfuse adapter. An exporter would be new code with no current caller; v0 observability is detection and reporting only. | SE-15, CTX-14 |
| NG-3 | No hosted or multi-tenant observability. | SE-5 |
| NG-4 | Do not fork, vendor, or re-implement the OpenSpec CLI, and do not materialize the dependency's provider prompt packs. The overlay is the schema, its templates, and the config — nothing that instructs a provider. Replacing the stock CLI to remove the Node dependency from the agent-facing workflow is a separate decision; its revisit condition is a user for whom installing Node is a real obstacle. | SE-4, SE-12, SE-19, CTX-6, CTX-7, CTX-19 |
| NG-5 | Do not change the announcement text, the reserved escalation path, question retention, intent binding, the fail-quiet posture, or the wrapper lifecycle. | SE-1, CTX-10 |
| NG-6 | Do not register the repository-scope scaffold at user scope, do not write a shared committed settings file that would install hooks for teammates, do not modify user-level scaffold configuration from `gr init` even to remove a legacy registration, and do not touch the owner's working `~/.claude/settings.json` or `~/.codex/config.toml` or branch `codex/dogfood-run-v0`. | SE-6, SE-7, SE-14 |
| NG-7 | Do not write, compute, reproduce, or simulate a scaffold trust record, and do not change how attachment trust is reported for the first scaffold. | SE-6, SE-16 |
| NG-8 | Do not delete a repository's committed harness files as part of disconnection. The overlay is version-controlled repository content; doctor reports its state and update repairs it, but removal stays the user's act. | SE-4, CTX-3 |
| NG-9 | Do not add a workflow engine, dashboard, database, service, credential store, or new dependency. | SE-1 |
| NG-10 | No `gr` command executes Node, npx, or the pinned CLI, and the absence of a Node runtime never fails `gr init`, `gr update`, or `gr doctor`. Verifying the canon against the pinned CLI happens on Goalrail's development side before the canon is embedded. | SE-18, SE-19, CTX-19 |
| NG-11 | This snapshot authorizes no implementation, commit, push, pull request, merge, archival, provider run, or external effect. Each remains a separate owner gate, recorded before the action. | SE-7 |

## Observable Success Signals

| ID | Signal | Measurement | Evidence |
|---|---|---|---|
| SIG-1 | A fresh directory becomes a working harness in one command, and the harness `gr` installs is one the pinned CLI accepts. | A test runs `gr init` in an empty directory; a development-side test then validates the materialized overlay with the pinned CLI and creates a change with the Goalrail schema successfully. | SE-1, SE-19, CTX-2, CTX-5 |
| SIG-2 | Initialization is idempotent. | A test runs `gr init` twice and compares every file byte for byte. | SE-1, CTX-1 |
| SIG-3 | An existing OpenSpec root survives initialization, and a foreign custom schema is never silently replaced. | A test seeds a spec and a change, initializes, and asserts both files are byte-identical while the config names the Goalrail schema; a test seeds a foreign-schema config and asserts initialization refuses without explicit confirmation. | SE-4, CTX-2 |
| SIG-4 | Drift is detected, never silently overwritten, and repaired only deliberately. | A test edits one materialized template, asserts `gr doctor` names that file as drifted, asserts `gr update` refuses and names it, then asserts the explicit flag restores the canonical digest. | SE-4, SE-20, CTX-2 |
| SIG-5 | The registration lands in the per-user project file and nowhere else, and only where Git ignores it. | A test runs initialization against a fake home and repository and asserts the per-user project settings file holds the managed handlers while the user-level configuration is byte-identical; a test makes the settings path unignored and asserts registration is blocked with a loud report. | SE-6, SE-14, CTX-13 |
| SIG-6 | Removal leaves no residue and no collateral, and re-initialization repairs rather than duplicates. | A test initializes, seeds a foreign handler, disconnects, and asserts the foreign handler survives intact while no managed handler or empty container remains; a test seeds a stale registration and asserts re-running `gr init` leaves exactly one current registration per event. | SE-6, CTX-9 |
| SIG-7 | The diagnosis distinguishes every state it claims to, in output and exit code. | A test drives `gr doctor` through: not initialized, overlay missing, overlay drifted, overlay behind the binary's canon, attachment absent, attachment registered, legacy user-scope registration present, Node absent, observability absent, and fully working — and asserts a distinct state, a next action for every failure, no next action for the optional absences, and an exit code that separates healthy from not. | SE-2, SE-16, SE-19, CTX-16 |
| SIG-8 | No remedy names a command that does not exist. | A test asserts `gr health` still runs and names `gr doctor`, and that every remedy string the code prints resolves to a command the CLI accepts. | SE-17, CTX-17 |
| SIG-9 | An update is legible and reversible. | A test asserts the update report names every rewritten file and the repository's move from old canon to new, and that the previous overlay is recoverable from what the command left behind. | SE-3, SE-20, CTX-2 |
| SIG-10 | Absent observability reads as fine, not as broken. | A test asserts that with no observability configured `gr doctor` reports the harness as working, prints one optional line, and prints no next action for it. | SE-5, CTX-15 |
| SIG-11 | The approval question leaves an evidence record. | One interactive session with the shipping registration is recorded under the change's evidence, stating what it establishes and what it does not, and the disclosure text is reconciled with it. | SE-6, CTX-10, CTX-11 |
| SIG-12 | `gr` never needs Node. | A test runs `gr init`, `gr update`, and `gr doctor` with an executable-lookup path holding no Node runtime and asserts every command completes, with the Node absence appearing only as doctor's environment line. | SE-18, SE-19, CTX-19 |

## Ambiguities and Unknowns

| ID | Question | Evidence |
|---|---|---|
| AMB-1 | Whether the second scaffold, in an interactive session, runs a repository-scoped registered hook with no approval step. Documentation records no such gate, which is enough to choose the arrangement; the observation is what upgrades the disclosure from documented to observed. Both answers produce the same registration, so this does not block confirmation. | CTX-11, CTX-12, CTXQ-1 |
| AMB-2 | Whether a harnessed repository also needs a committed instruction file naming the pinned invocation and the explicit-schema workaround, or whether `gr init`'s report and `gr doctor` printing it is enough. This version answers "printing it is enough" and writes no instruction file. Revisit condition: an agent in a freshly initialized repository still guesses the invocation. | SE-10, SE-12, CTX-5, CTX-7 |

## Confirmation

- **Confirmed by:** repository owner
- **Confirmed at:** 2026-07-30T13:50:00Z
- **Verification action:** The owner reviewed a plain-language view of candidate version 1 in the resolved owner language, directed a critical review of it, and then reviewed version 2 in the same form. Version 2 was amended on four owner decisions given in that exchange: `gr` must not depend on a Node runtime, `gr update` stops on drift by default and overwrites only on an explicit flag, overlay currency is judged by digests rather than a version recorded in the repository, and initialization selects the scaffold by detection with a flag override. The owner then asked whether OpenSpec — and therefore Node — is unavoidable, was shown the split (Goalrail's own paths parse the artifacts in Go and never invoke the CLI; only the stock OpenSpec CLI's own `new change`, `status`, `validate`, and `archive` need Node), confirmed the terminology that "stock CLI" means the published OpenSpec package rather than `gr`, and explicitly confirmed version 2.
- **Amendment note:** Version 1 is retained as `intent-v1.md`. This version supersedes it; the earlier evidence is not rewritten.
- **Amendment rule:** A material change to outcomes, non-goals, or success signals creates a new version; wording-only edits preserve this version.
