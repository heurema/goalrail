# Context Pack

- **Context Pack ID:** context-verify-intent-context-binding-v0
- **Version:** 2
- **Previous version:** 1
- **Started at:** 2026-07-25T08:10:00Z
- **Completed at:** 2026-07-25T16:05:00Z
- **Outcome:** sufficient

## Context Items

| ID | Kind | Claim | Source | Observed at | Relevance |
|---|---|---|---|---|---|
| CTX-1 | repository | Preparation resolves the sibling `context.md` declared by a confirmed intent, confines it to the canonical repository root, applies the intent size bound, validates the resulting flow snapshot, and fails closed on missing, unreadable, mismatched, or symlink-escaping context before any state is written. | git:codex/bind-intent-context-v0@b36fabd | 2026-07-25T08:12:00Z | The behaviour is introduced by this change and therefore must be covered by its confirmed intent, not treated as pre-existing. |
| CTX-2 | repository | The promoted `local-run` capability never mentions the Context Pack. Its `Run preparation is inspectable and does not launch` requirement lists the confirmed intent reference only as one invalid-input case in a scenario. | repo:openspec/specs/local-run/spec.md@32913f19 | 2026-07-25T08:14:00Z | Identifies the exact promoted requirement that must be modified and confirms nothing is being duplicated. |
| CTX-3 | repository | The only prior wording describing this enforcement lives in the unmerged local change `activate-dogfood-run-v0`, embedded inside a `MODIFIED Requirements` block for `Real activation remains separately gated`, which `main` rewrote in PR #12. | repo:openspec/changes/activate-dogfood-run-v0/specs/local-run/spec.md | 2026-07-25T08:20:00Z | The enforcement must be extracted into its own requirement instead of reusing an entangled and superseded block. |
| CTX-4 | repository | That same unmerged change declares `intent-activate-dogfood-run-v0` version 1 as confirmed, while the archived change merged into `main` declares the identical intent ID and version with different confirmed wording. | repo:openspec/changes/archive/2026-07-24-activate-dogfood-run-v0/intent.md@32913f19 | 2026-07-25T08:20:00Z | The existing intent ID cannot be reused or re-versioned without rewriting merged evidence, so this change requires a new intent identity. |
| CTX-5 | repository | The promoted `intent-snapshot` capability requires a flow Intent Snapshot to reference a valid Context Pack and requires every derived item to carry resolvable context IDs, but states no enforcement at WorkSpec preparation time. | repo:openspec/specs/intent-snapshot/spec.md | 2026-07-25T08:16:00Z | Confirms the gap is preparation-time enforcement only; the semantic contract already exists and must not be restated. |
| CTX-6 | repository | The promoted `context-pack` capability defines Context Pack structure and lifecycle without stating when preparation must enforce it. | repo:openspec/specs/context-pack/spec.md | 2026-07-25T08:16:00Z | Confirms the same gap from the Context Pack side and bounds this change to the preparation boundary. |
| CTX-7 | repository | Remote `main` advanced to the merged hook-linkage fix, which modifies the `Run lineage uses provider-authoritative identity` requirement and leaves `Run preparation is inspectable and does not launch` untouched. | git:origin/main@6c1f022f | 2026-07-25T16:00:00Z | The delta in this change still targets a requirement no other merged change has altered. |
| CTX-8 | external | The owner directed reconciliation onto merged `main` first, shipped the hook-linkage fix separately, and then authorized fixing every confirmed review finding on this change before approval. | owner:goalrail-review-remediation-2026-07-25 | 2026-07-25T15:55:00Z | Defines the current scope as remediation of confirmed findings and keeps merge, archival, and deployment behind later gates. |
| CTX-9 | repository | Version 1 of this snapshot declared the change specification-only and listed implementation changes as a non-goal, while the change itself introduces `readResolvedIntent`. The referenced earlier implementation commit is not in the ancestry of `main`. | repo:openspec/changes/verify-intent-context-binding-v0/intent-v1.md | 2026-07-25T15:50:00Z | The snapshot must cover the implementation it ships; this is the material amendment that creates version 2. |
| CTX-10 | repository | `changeRequiresContext` treats a non-archived change under the project intent schema as always requiring a Context Pack, and an archived change as requiring one only when its intent declares it. Preparation applied no equivalent rule, so omitting both the declaration and the artifact bypassed verification. | repo:internal/adapters/openspec/adapter.go | 2026-07-25T15:52:00Z | Preparation must apply the same schema-aware distinction, and the requirement must state it instead of allowing an unconditional no-pack path. |
| CTX-11 | repository | `Service.Prepare` freezes the WorkSpec before invoking the intent verifier, so verification cannot occur before freezing. Prepared state is persisted only after both steps succeed. | repo:internal/localrun/service.go | 2026-07-25T15:53:00Z | The requirement must state the boundary that actually holds: verification precedes persisted prepared state, not freezing. |
| CTX-12 | repository | Opening a FIFO named `context.md` blocks until a writer connects, so a bounded reader can wait indefinitely and preparation hangs instead of failing with a bounded reason. A stat before opening distinguishes a regular file without blocking. | local:fifo-open-probe-2026-07-25T160000Z | 2026-07-25T16:00:00Z | Both bounded artifact reads must reject a non-regular artifact before opening it. |
| CTX-13 | repository | A Context Pack source cell is parsed as exactly one evidence reference, and the accepted pattern admits no space or semicolon. Two rows in version 1 packed two references into one cell and were therefore invalid. | repo:internal/domain/validation.go | 2026-07-25T15:54:00Z | This pack must carry one valid reference per row, otherwise the change's own intent cannot pass the verification it introduces. |

## Material Unknowns

None.
