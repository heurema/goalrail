# Context Pack

- **Context Pack ID:** doctor-toolchain-inspection-v0
- **Version:** 1
- **Previous version:** pending
- **Artifact Contract:** goalrail-context-intent
- **Artifact Contract Version:** 1
- **Started at:** 2026-08-06T17:00:00Z
- **Completed at:** 2026-08-06T17:58:12Z
- **Outcome:** sufficient

## Context Items

| ID | Kind | Claim | Source | Verification recipe | Observed at | Relevance |
|---|---|---|---|---|---|---|
| CTX-1 | repository | `gr doctor` executes the declared planning runtime and the declared planning adapter, each with `--version`, on every run in a canon-initialized project. | `internal/doctor/planning.go:70,75,95` | Build `gr`; `git init` a scratch directory and run `gr init --repo .`; place executables named `node` and `openspec` earlier on `PATH` that append a line to a marker file and print `22.18.0` and `1.6.0`; run `gr doctor --repo .`. Expect the marker to contain one line for each. | 2026-08-06T17:20:00Z | Establishes the divergence as executed behaviour rather than as a reading of the code. |
| CTX-2 | repository | The canon setup profile declares `runtime: node` and adapter id `openspec`, so CTX-1 executes exactly the two programs a promoted requirement names. | `internal/project/canon/setup-profile.json.tmpl`; `internal/project/canon/planning-adapter.json` | Read both files; expect `"runtime":"node"` and `"id":"openspec"`. | 2026-08-06T17:15:00Z | Without this the execution in CTX-1 could be of some unrelated program and would not meet the named prohibition. |
| CTX-3 | repository | A promoted requirement forbids initialization, update, and diagnosis from executing Node, a package runner, or the stock OpenSpec CLI, and states the prohibition as a violation test rather than a preference. | `openspec/specs/harness-init/spec.md:258,279` | Read the requirement and the scenario `A Goalrail command would invoke the checking CLI`. | 2026-08-06T16:50:00Z | This is the requirement CTX-1 violates. |
| CTX-4 | repository | Diagnosis is nevertheless required to state whether every declared planning runtime and pinned compiler is available and compatible, distinguishing a missing executable, an incompatible version, an unavailable package, and an unverified integrity identity. | `openspec/specs/harness-doctor/spec.md:79,84` | Read the requirement and its four named states. | 2026-08-06T16:52:00Z | Removing the execution without replacing the fact-finding would violate this requirement instead. The fourth state names integrity as an accepted basis for the verdict. |
| CTX-5 | repository | Goalrail's own setup installs a private pinned Node 22.18.0 and OpenSpec 1.6.0, with a SHA-256 recorded per platform archive. | `release/setup/source-lock.json` | Read the file; expect `runtime.version` `22.18.0`, `compiler.version` `1.6.0`, and a `runtime_sha256` on each platform entry. | 2026-08-06T17:35:00Z | The toolchain whose availability must be reported is one Goalrail places itself, so its identity is known before any inspection. |
| CTX-6 | repository | Authorized setup installs that bundle at `<home>/.local/share/goalrail/bundles/<release-version>/<os>_<arch>/`, writes the bundle manifest into that root, and verifies every installed file against it before completing. | `internal/setup/apply.go:234,376,380,383`; `internal/setup/plan.go:612`; `internal/releasebundle/contracts.go:51` | Read the destination construction, the manifest write, and the `verifyInstalledBundle` call and body. | 2026-08-06T17:40:00Z | The durable location and a per-file digest record already exist on disk after setup; no new artifact is required to learn them. |
| CTX-7 | repository | The bundle manifest names the runtime executable at the fixed path `runtime/node/bin/node` and the compiler entrypoint under `node_modules/@fission-ai/openspec`, each with a digest. | `internal/setup/plan.go:499`; `internal/releasebundle/sources.go:270`; `release/setup/source-lock.json` | Read the manifest lookups and the source-lock `entrypoint` field. | 2026-08-06T17:41:00Z | Identifies the exact files whose digests answer the availability question without executing them. |
| CTX-8 | repository | A release refuses to publish when an artifact's stamped version differs from the pushed tag, so a `gr` installed from a bundle reports exactly that bundle's release version. | `internal/releasebundle/build.go:153-154`; `openspec/specs/release-channel/spec.md:121` | Read the equality check in the release builder and the promoted requirement it implements. | 2026-08-06T17:50:00Z | Makes the installed bundle's directory derivable from the running binary's own version, so no pointer artifact and no new discovery contract is needed. |
| CTX-9 | repository | The stable executable at `<home>/.local/bin/gr` is a byte copy of the bundle's `bin/gr`, not a link, so it carries no reference back to the version root it came from. | `internal/setup/apply.go:389-425` | Read `selectStableExecutable`; expect a temporary-file copy and rename, with no symlink creation. | 2026-08-06T17:45:00Z | Rules out the filesystem link as the discovery route and leaves CTX-8 as the available one. |
| CTX-10 | repository | Planning readiness gates local readiness, so a component that can never reach a ready state would leave every managed project reporting setup required. | `internal/doctor/doctor.go:219-224,362-363` | Read the `LocallyReady` and `Planning.Ready` conjunctions. | 2026-08-06T17:10:00Z | Bounds the acceptable replacement: it must be able to reach a positive verdict, not only a safe one. |
| CTX-11 | repository | The diagnosis already defines an `unverified_integrity` component state and currently uses it only for the Goalrail bundle component, never for the runtime or the compiler. | `internal/doctor/planning.go:20,64` | Read the constant and grep the package for its uses. | 2026-08-06T17:12:00Z | The state the replacement needs already exists in the machine contract, so consumers gain no new vocabulary. |
| CTX-12 | repository | The full test suite passes at `23e30a5` with the executing behaviour in place. | `go test ./...` at `23e30a5` | Check out `23e30a5` and run `go test ./...`; expect no `FAIL` line. | 2026-08-06T17:05:00Z | The divergence is not covered by any existing test, so the replacement must bring its own regression or it can return unnoticed. |
| CTX-13 | repository | A machine can carry a system Node whose version does not satisfy the declared pin; the owner's machine carried `v25.9.0` against a declared `22.18.0`. | `node --version` on the owner's machine | Run `node --version`. This measures the present machine and does not reproduce the recorded observation; expect any version, which may or may not equal the pin. | 2026-08-06T17:55:00Z | A `PATH` lookup answers about an arbitrary installation rather than about the one the declared profile pins, which is a second defect independent of the execution. |
| CTX-14 | repository | The declared runtime name is repository content whose validation bounds only length, NUL, and secret-shaped content, so it can carry a path rather than a program name. | `internal/domain/setup.go:430,665-673` | Read `validatePlanningAdapter`'s call for `planning.runtime` and the body of `validateSetupString`. | 2026-08-06T17:18:00Z | Explains why the execution in CTX-1 runs a program the checked-out repository chooses, which the replacement removes by not deriving a path from repository content at all. |
| CTX-15 | repository | `go install` remains a documented installation route, so a managed project may be diagnosed by a released binary with no Goalrail-installed bundle present. | `openspec/specs/release-channel/spec.md:198` | Read the requirement's `go install` sentence. | 2026-08-06T17:56:00Z | Fixes the population whose reported readiness changes: an installation that never ran setup has no bundle to verify, and must be reported as setup required rather than ready. |

## Material Unknowns

None.
