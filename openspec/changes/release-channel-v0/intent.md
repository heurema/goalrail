# Intent Snapshot

- **Intent ID:** intent-release-channel-v0
- **Version:** 2
- **Status:** confirmed
- **Owner:** repository owner
- **Context Pack:** context-release-channel-v0 version 2
- **Run references:** pending
- **Previous versions:** 1 (`intent-v1.md`), candidate, never confirmed. Amended after an owner-directed critical review found five material defects: the artifact-naming rationale contradicted the version mechanism the same version adopts; the macOS claim was false for the very install route the change exists to enable; the motivating agent journey was covered by no outcome; the release's own build outputs would have marked its artifacts dirty and failed its own gate; and both verification mechanisms named for the version were unimplementable as written.

## Source Evidence

- **SE-1 (owner):** The README documents installation as `go install`, which requires a Go toolchain on the user's machine. The landing page being planned installs Goalrail by handing a prompt to an agent, and that prompt should not require Go. A release channel is what closes the gap.
- **SE-2 (owner):** The slice is narrow: continuous integration, a release workflow producing binaries for the platforms that build today, a version that comes from the tag rather than a constant, and the README's install documentation updated to name the binaries.
- **SE-3 (owner):** A workflow of our own, not goreleaser. Four static binaries, no CGO, no signing, no package managers is four `go build` invocations plus a checksum file; goreleaser would add a configuration DSL and an external action for work that does not exist here. Revisit when a Homebrew tap or artifact signing becomes wanted.
- **SE-4 (owner):** Windows is unsupported, with the reason recorded rather than the gap left silent. Supporting it means deciding how to keep the hygiene guarantee its missing flags implement, not adding a matrix entry. Revisit on a real user on Windows.
- **SE-5 (owner):** This goes through the full OpenSpec cycle: it changes what a version means, satisfies a recorded revisit condition, and creates the distribution boundary the product is installed through.
- **SE-6 (owner):** Explicitly outside this slice, each for its own reason: the update-availability check in `gr doctor`, because this slice creates the channel and querying it is a separate change; Windows; and signing, notarization, SBOM, a Homebrew tap, and self-update, which have no demand yet and each carry their own decision.
- **SE-7 (owner):** Creating the first tag and publishing a release are external actions, each keeping its own owner gate; neither is granted by the brief or by confirming this intent. Nothing here authorizes touching the owner's working scaffold configuration, and the deferred branch `codex/dogfood-run-v0` stays untouched.
- **SE-8 (owner):** A user who downloads an unsigned macOS binary through a browser meets a Gatekeeper warning that `go install` never produces. Not a blocker, but the install documentation should say it rather than let the user discover it (CTX-15).
- **SE-9 (repository):** The repository has no continuous integration of any kind, and its whole verification contract is the Go toolchain: `go vet ./...` clean, `go test ./...` green across 13 packages in about 12 seconds, with no dependency to download (CTX-1, CTX-7).
- **SE-10 (repository):** Four platforms build with `CGO_ENABLED=0` and `windows/amd64` does not, because `syscall.O_NOFOLLOW` is undefined there — and those flags are how a promoted hygiene guarantee is implemented, so a build-tagged stub would compile and drop the guarantee silently (CTX-5, CTX-6).
- **SE-11 (repository):** The version is the constant `"0.1.0"`, reported by `gr version` and `gr doctor`, and no verdict about a repository derives from it (CTX-3).
- **SE-12 (repository, verified; corrected in this version):** Go stamps a version into every build, and every stamped shape carries a leading `v`: a clean checkout at tag `v0.1.0` yields `v0.1.0`, even from a shallow single-ref clone; one commit later yields the next patch as a pseudo-version, `v0.1.1-0.<timestamp>-<commit>`; with no tags at all, `v0.0.0-<timestamp>-<commit>`; a dirty tree adds `+dirty` to whichever shape applies, including `v0.1.0+dirty` exactly at a tag. Version 1 of this snapshot said `(devel)` appears "only when VCS stamping is off"; that is true of `go build` but not of test binaries, which report `(devel)` in every Git state (CTX-4, CTX-17).
- **SE-13 (repository):** The one development-side check that the stock CLI accepts the embedded canon skips — never fails — when the pinned package is not in the npm cache, because it probes with `--offline` so an ordinary `go test` never reaches the network. On a fresh runner the cache is empty, so the check would not run and the release would be green without it (CTX-8).
- **SE-14 (repository):** A promoted requirement forbids `gr update` from querying a release channel and justifies the prohibition by the channel's absence; the archived `harness-init-v0` records "Revisit when a release channel exists" as the condition this change satisfies (CTX-9, CTX-10).
- **SE-15 (repository):** With zero tags, `go install …@latest` installs the branch tip; after the first tag it installs that tag. The README documents that command as the only route, repeats it inside the prompt handed to an agent, and states that no binaries are published (CTX-2, CTX-12).
- **SE-16 (repository):** The repository is public, MIT-licensed, and has Actions enabled with all actions allowed; the licence requires its notice to accompany a distributed copy, and `go.mod` declares the toolchain the workflow must resolve (CTX-11, CTX-12, CTX-13).
- **SE-17 (repository, verified this session):** A build's own output marks the tree dirty. At a tagged checkout the first build into `./dist` stamps `v0.1.0` and every later build stamps `v0.1.0+dirty` — including one written outside the tree, because the earlier artifact still sits inside it. Removing the directory restores the clean stamp, and `.gitignore` carries no build-output entry (CTX-16).
- **SE-18 (repository, verified this session):** What can verify a stamped version is bounded: a test binary reports `(devel)` in every Git state, so only injected build information can exercise the shapes; `go version -m` reads the stamp out of any platform's binary without executing it; and a tag-following fetch that pulls a second tag on the same commit stamps the higher one, while a single-ref `--no-tags` fetch stamps the requested tag (CTX-17).
- **SE-19 (repository, verified this session):** The macOS warning belongs to one path only. `curl` sets no quarantine attribute and `tar` does not carry one onto an extracted file, so a command-line download runs with no prompt; the warning is the browser-and-Finder path. The darwin/arm64 binary is ad-hoc signed by the Go linker — "not notarized" is the accurate word, not "unsigned" — that signature is load-bearing on Apple Silicon, and Control-click "Open" stopped overriding Gatekeeper in macOS Sequoia (CTX-18).
- **SE-20 (repository):** A registration names the running executable's symlink-resolved absolute path, and the diagnosis fails when nothing runnable remains there. A binary run from a temporary download directory produces an attachment that breaks when the directory is cleaned (CTX-19).
- **SE-21 (repository, verified this session):** The obvious verification command fails on a partial download: `shasum -a 256 -c checksums.txt` exits `1` when it lists archives the user did not fetch, and only the `--ignore-missing` form exits `0` (CTX-20).
- **SE-22 (external, verified this session):** GitHub serves a stable `releases/latest/download/<asset>` address only for an asset whose name does not change between releases, so under version-bearing archive names the checksums file is the only stable entry point (CTX-21).
- **SE-23 (external, verified this session):** `gh release create` already creates a draft, uploads the assets, and publishes only afterwards, and a published release is immutable — so all-or-nothing publishing is the default behaviour of the preinstalled tool, and a botched release is superseded by a new tag rather than repaired in place (CTX-22).
- **SE-24 (repository, verified this session):** `go test` exits `0` when the canon check skips, while `go test -json` marks that test `skip` in the same run, so a workflow can fail on a skipped check without changing the test or adding a dependency (CTX-23).
- **SE-25 (external, verified this session):** `main` has no branch protection or rulesets, and the repository's default workflow permission is `write`: what a check can promise here is visibility, not a blocked merge, and each workflow should declare the narrow permission it needs (CTX-24).
- **SE-26 (repository):** Archived changes are history; a recorded revisit condition cannot be marked satisfied by editing the archive (CTX-25).
- **SE-27 (owner, this session):** The owner directed a critical review of candidate version 1 before confirming anything, and directed that its confirmed findings be folded into a new version rather than repaired silently.

## Desired Outcomes

| ID | Confirmed wording | Verification action | Evidence |
|---|---|---|---|
| OUT-1 | Every pull request and every push to `main` is checked automatically: the project's own verification — vet and the full test suite — runs on both operating systems it ships binaries for, and every platform the documentation claims is compiled. A change that breaks a supported platform's build stops being a release-time discovery and becomes a failed check that is visible before merge. What the check promises is visibility: this repository has no branch protection, and configuring one would be its own decision. | Run the same commands the workflow runs locally and confirm they reproduce today's green baseline and the Windows failure; then read the check result on the first pull request that carries the workflow, which is where the guarantee is actually observable. | SE-2, SE-9, SE-10, SE-25, CTX-1, CTX-5, CTX-7, CTX-24 |
| OUT-2 | Pushing a version tag publishes a release: one archive per supported platform, each carrying the binary and the licence, and one checksums file covering them all. Nothing is published unless every platform built, the checks passed, and every artifact carries the tag it claims — so a partial or mislabelled release cannot exist. The build produces artifacts without dirtying the tree it builds from, because an artifact left inside the checkout marks every later build as modified. Publication itself is done by the platform's own preinstalled tooling, which stages a draft and publishes only after every asset is uploaded. | Read the workflow's job graph and where it writes artifacts; on the first real tag, download the published archives, verify them against the checksums file, and run the extracted binary. | SE-2, SE-3, SE-16, SE-17, SE-23, CTX-5, CTX-11, CTX-16, CTX-22 |
| OUT-3 | The version a binary reports is the version it was actually built from, reported exactly as the build carries it, with no rewriting on the way out. A released binary reports its tag, `v` and all; a build between tags reports the next patch version as a pseudo-version naming the commit; a tree with uncommitted changes says so; a build with no version control behind it reports the toolchain's own development marker rather than inventing a number. One string is used everywhere — in the tag, in what the tool prints, and in the artifact names — so nothing has to be normalized, matched loosely, or explained. A second release can never report the first release's number. | Build the binary in each situation — at a tag, after a tag, from a dirty tree, and with version stamping off — and read what `gr version` prints in each, confirming it is exactly what the build carries. | SE-2, SE-11, SE-12, SE-18, CTX-3, CTX-4, CTX-17 |
| OUT-4 | The check that the embedded canon is one the stock CLI accepts actually runs before a release, instead of skipping into a green result. The workflow makes the pinned package available and fails when the check did not execute, so a canon can never ship unverified — while a contributor without the runtime still gets a loud skip rather than a broken build, and the harness itself gains no new switch. | Read the workflow log for the evidence that the check executed; remove the priming step in a scratch run and confirm the job fails instead of passing quietly; run the suite locally without the runtime and confirm it still skips. | SE-13, SE-24, CTX-8, CTX-23 |
| OUT-5 | Installation stops requiring a Go toolchain and the documentation says exactly how: which archive to download for which platform, the exact command that verifies it against the checksums file when only one archive was fetched, and that Windows is not supported together with the reason. The macOS note states what is true rather than a generic scare: an archive downloaded through a browser and opened in Finder carries a quarantine flag and meets Gatekeeper, the documented command-line download does not, the binaries are not notarized, and the remedy given is one that still works. `go install` remains documented as the other route, with the note that after the first tag it installs the release rather than the branch tip. | Follow the README's own download instructions on a machine with no Go installed, verify the checksum, run the binary; separately download the same archive through a browser, extract it in Finder, and confirm the documented warning and the documented remedy are what actually happen. | SE-1, SE-4, SE-8, SE-15, SE-19, SE-21, CTX-2, CTX-12, CTX-15, CTX-18, CTX-20 |
| OUT-6 | The promoted requirements stay true the day the channel exists. The rule that `gr` performs no network lookup for a newer release survives exactly as it is, but stops resting on "there is no channel" and rests on the deliberate boundary instead. That the recorded revisit condition is now satisfied is stated in this change's own artifacts and in the promoted requirement that carried the reasoning; the archived change that recorded the condition is history and is not edited. | Read the updated requirement and confirm the prohibition is unchanged while its stated reason is the boundary rather than the absence of a channel; confirm the archive is untouched. | SE-6, SE-14, SE-26, CTX-9, CTX-10, CTX-25 |
| OUT-7 | The route this change exists for works end to end: the prompt a user hands to an agent installs Goalrail on a machine with no Go, and the README's status stops saying no binaries are published. Because there is no install script, the documentation itself carries the three steps an agent cannot guess: how to find the current release without knowing its version, given that only a name which does not change between releases has a stable address; where to put the binary so it keeps working, because the attachment records the absolute path of the executable that installed it and the diagnosis fails when nothing is there; and the verification command that succeeds when one archive out of several was downloaded. The prompt permits that one placement explicitly, rather than forbidding every write outside the repository and leaving the agent to improvise. | Hand the prompt to an agent on a machine with no Go and let it install, initialize, and report the diagnosis; then delete nothing and re-run the diagnosis after the download directory would ordinarily have been cleaned. The run itself is a provider run and keeps its own owner gate. | SE-1, SE-15, SE-20, SE-21, SE-22, CTX-12, CTX-19, CTX-20, CTX-21 |

## Non-Goals

| ID | Confirmed boundary | Evidence |
|---|---|---|
| NG-1 | No update-availability check and no self-update. No `gr` command queries the channel, resolves a latest version, or replaces the binary. This change creates the channel; asking it a question is the separate change the revisit condition now unblocks. | SE-6, SE-14, CTX-9, CTX-10 |
| NG-2 | No Windows binary and no build-tagged stub for the platform-specific flags. The gap is recorded with its reason and its revisit condition — a real user on Windows — rather than closed by code that compiles while dropping a promoted guarantee. | SE-4, SE-10, CTX-5, CTX-6 |
| NG-3 | No goreleaser and no third-party release framework. Revisit when a Homebrew tap or artifact signing becomes wanted, which is where such a tool earns its keep. | SE-3 |
| NG-4 | No signing, notarization, SBOM, build provenance, Homebrew tap, install script, package manager, or container image. The quarantine behaviour is documented accurately, not engineered away. No post-link processing of a published binary either: the arm64 signature the Go linker writes is load-bearing, so what the linker produced is what ships. | SE-6, SE-8, SE-19, CTX-18 |
| NG-5 | No change to how a repository's state is judged. Digests still decide whether an overlay is current; the version stays information about the binary and no repository verdict derives from it. | SE-11, CTX-3 |
| NG-6 | No change to the harness itself: not the embedded canon, the overlay, the escalation loop, the diagnosis, or any runtime behaviour beyond the version string the tool reports. In particular the canon check gains no new switch inside the harness; whatever makes a skipped check fail lives in the workflow. | SE-2, SE-5, SE-24 |
| NG-7 | No new module dependency, service, dashboard, or credential. The build stays hermetic and dependency-free, and the workflow uses the platform's own credentials rather than a stored secret, declaring the narrow permission it needs rather than inheriting a broad default. | SE-9, SE-25, CTX-7, CTX-11, CTX-24 |
| NG-8 | This snapshot authorizes no implementation, commit, push, pull request, merge, tag, release publication, or other external effect. Creating the first tag and publishing the first release each keep their own owner gate, recorded before the action. The owner's working scaffold configuration and the deferred branch `codex/dogfood-run-v0` stay untouched. | SE-7, CTX-14 |
| NG-9 | Do not change how a registration records the executable. The attachment binding the absolute path of the binary that installed it is existing, promoted behaviour; this change adapts the install instructions to it and does not rewrite it to tolerate a binary that moves. Revisit condition: a user who installs by download reports a broken attachment after an ordinary cleanup. | SE-20, CTX-19 |

## Observable Success Signals

| ID | Signal | Measurement | Evidence |
|---|---|---|---|
| SIG-1 | A platform the documentation claims cannot break silently. | The workflow compiles exactly the four platforms the README names, and the same commands run locally reproduce four successes and the `windows/amd64` failure, which stays outside the matrix by decision. | SE-10, CTX-5 |
| SIG-2 | The reported version follows the build, and a release cannot publish an artifact that says otherwise. | A test asserts the resolver over injected build information covering every recorded shape — exact tag, tag with local modifications, pseudo-version, development marker, and absent information — because a test binary itself reports the development marker in every Git state. The release then reads each of the four artifacts' stamps without executing them and requires exact equality with the pushed tag, which is what catches a tree dirtied by the build's own output. | SE-12, SE-17, SE-18, CTX-4, CTX-16, CTX-17 |
| SIG-3 | A release is all or nothing, and verifiable by whoever downloads it. | Publishing depends on every platform's build and on the checks passing; the published set is four archives plus one checksums file, and the documented verification command succeeds against a single downloaded archive as well as against all four. | SE-2, SE-16, SE-21, CTX-5, CTX-20, CTX-22 |
| SIG-4 | The canon guarantee is never green by omission. | The workflow fails when the pinned-CLI check reports as skipped; with the priming step removed the job fails; locally without the runtime the same test still skips loudly, and no harness file changed to make this true. | SE-13, SE-24, CTX-8, CTX-23 |
| SIG-5 | The documentation and the release agree on every name. | Each artifact name quoted in the README matches a name the workflow produces, checked against the first release's actual asset list before the download instructions are treated as true. | SE-1, SE-15, CTX-12 |
| SIG-6 | Installing without Go works on the route the change exists for, and the older route keeps working with its meaning stated. | An agent handed the README's prompt on a machine with no Go installs, initializes, and reports a healthy diagnosis — including after the download location would ordinarily have been cleaned — and `go install …@latest` still installs, resolving to the tag rather than the branch tip, as the documentation now says. | SE-1, SE-15, SE-20, CTX-2, CTX-19 |
| SIG-7 | No promoted requirement asserts something the release channel has made false, and no history is rewritten to achieve that. | Strict validation passes, the updated `harness-update` requirement states the prohibition without resting on the channel's absence, and the archived change is byte-identical. | SE-14, SE-26, CTX-9, CTX-25 |

## Ambiguities and Unknowns

| ID | Question | Evidence |
|---|---|---|
| AMB-1 | Which version the first tag carries. Proposed: `v0.1.0`, matching the constant the code already carries — nothing has been published, so no version has been claimed publicly and starting elsewhere would need a reason. Resolved by owner confirmation of this version. | SE-11, CTX-2, CTX-3 |
| AMB-2 | The artifact naming scheme, which outlives this change because the documentation, the landing page, and any install script quote it. Proposed, and changed from version 1: `gr_v0.1.0_darwin_arm64.tar.gz` — the tag verbatim, leading `v` included — plus one `checksums.txt`, with each archive holding the `gr` binary and the licence. Version 1 proposed stripping the `v` on the grounds that the name would then contain exactly what `gr version` prints; that rationale was false, because every version Go stamps carries the `v`. Keeping it makes the claim true instead: one string in the tag, in the binary's own report, in the filename, and in the release gate's comparison, with nothing to normalize. Resolved by owner confirmation of this version. | SE-2, SE-12, SE-16, SE-18, CTX-4, CTX-12, CTX-17 |
| AMB-3 | Whether continuous integration runs on pull requests as well as on the release tag. Proposed: on pull requests and pushes to `main` as well as on the tag, because pull-request coverage is what would have caught the Windows build failure before it became a release-time discovery. Resolved by owner confirmation of this version. | SE-9, CTX-1, CTX-5 |
| AMB-4 | Whether the README's download instructions may be merged before the first release exists, since they would name archives nobody can fetch yet. Proposed: they land in this change and the first tag follows the merge as its own gated act, keeping the gap to the length of one release run rather than splitting the documentation across two changes. Resolved by owner confirmation of this version. | SE-7, SE-15, CTX-12 |

## Confirmation

- **Confirmed by:** repository owner
- **Confirmed at:** 2026-07-30T16:01:00Z
- **Verification action:** The owner directed a critical review of candidate version 1 rather than confirming it, read the plain-language view of what that review established — the false naming rationale, the macOS claim that does not hold on the documented route, the uncovered agent journey, the build outputs that would dirty the release's own artifacts, and the two unimplementable verification mechanisms — and then reviewed a plain-language view of version 2 in the resolved owner language, covering its outcomes, boundaries, signals, and the four proposed ambiguity answers including the reversal on the leading `v` in artifact names. The owner explicitly confirmed version 2.
- **Amendment note:** Version 1 is retained as `intent-v1.md`. This version supersedes it and states what changed and why; the earlier record is not rewritten.
- **Amendment rule:** A material change to outcomes, non-goals, or success signals creates a new version; wording-only edits preserve this version.
