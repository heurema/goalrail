# 12 Memory Update

No long-term memory update requested.

Potential future memory, only if the user explicitly asks to persist it:

- For Omnigent codebase-memory integration, prefer companion-tool installer/setup
  integration over vendoring or Python dependency side effects.
- Do not run `codebase-memory-mcp install -y` blindly from Omnigent until CBM has
  a non-destructive repair path or Omnigent prompts because current CBM install
  can auto-delete existing index DBs under `-y`.

## Promote to memory

| Target | Item | Evidence rows | Confidence |
|---|---|---|---|

## Do not promote

| Item | Reason |
|---|---|

## Memory files touched
